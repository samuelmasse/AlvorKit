{{SourceHeader}}namespace {{Namespace}};

/// <summary>
/// A <c>{{NativeLibrary}}</c> wrapper for <see cref="{{ApiClass}}"/> that forwards every call to an inner instance.
/// Override only the calls you want to intercept; the rest pass straight through.
/// </summary>
[global::System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
public class {{ApiClass}}Wrapper({{ApiClass}} inner) : {{ApiClass}}
{
    /// <summary>The <c>{{NativeLibrary}}</c> API instance each call is forwarded to.</summary>
    protected {{ApiClass}} Inner { get; } = inner ?? throw new ArgumentNullException(nameof(inner));
{{Methods}}}
