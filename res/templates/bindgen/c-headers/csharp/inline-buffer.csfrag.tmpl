
    /// <summary>A fixed buffer of {{Count}} <see cref="{{ElementType}}"/> values.</summary>
    [InlineArray({{Count}})]
    public struct {{ManagedName}}
    {
        /// <summary>First element storage used by the compiler-expanded inline array.</summary>
        private {{ElementType}} element;
    }
