    public virtual void {{ManagedName}}({{Signature}})
    {
        var pointer = this.{{ManagedName}}({{CallArgs}});
        if (pointer == 0) { result = default; return; }
        var bytes = MemoryMarshal.CreateReadOnlySpanFromNullTerminated((byte*)pointer);
        System.Text.Unicode.Utf8.ToUtf16(bytes, destination, out _, out var written);
        result = destination[..written];
    }
