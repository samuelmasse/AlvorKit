{{SourceHeader}}namespace {{Namespace}};

/// <summary>
/// A <see cref="{{ApiClass}}"/> wrapper for native OpenGL commands from <c>gl.xml</c>.
/// Subclass it and override only the calls you want to intercept; the rest pass straight through.
/// </summary>
[global::System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
public class {{ApiClass}}Wrapper({{ApiClass}} inner) : {{ApiClass}}
{
    /// <summary>The <c>gl.xml</c> OpenGL API instance each call is forwarded to.</summary>
    protected {{ApiClass}} Inner { get; } = inner ?? throw new ArgumentNullException(nameof(inner));
{{Methods}}}
