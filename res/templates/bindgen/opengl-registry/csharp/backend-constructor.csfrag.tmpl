
    /// <summary>Resolves every entry point through <paramref name="getProcAddress"/> while the context is current.</summary>
    public {{BackendClass}}(Func<string, nint> getProcAddress)
    {
{{Assignments}}    }
