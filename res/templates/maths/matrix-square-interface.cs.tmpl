// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>Applies to square matrix types, including <see cref="Mat2" />, <see cref="Mat3" />, and <see cref="Mat4d" />.</summary>
/// <typeparam name="TSelf">The square matrix type, such as <see cref="Mat3" /> or <see cref="Mat4d" />.</typeparam>
/// <typeparam name="TScalar">The component type, such as <see cref="float" /> or <see cref="double" />.</typeparam>
/// <typeparam name="TColumn">The column and row vector type.</typeparam>
public interface IMatSquare<TSelf, TScalar, TColumn> :
    IMatNumeric<TSelf, TScalar, TColumn, TColumn, TSelf>,
    IMultiplicativeIdentity<TSelf, TSelf>,
    IMultiplyOperators<TSelf, TSelf, TSelf>
    where TSelf : struct, IMatSquare<TSelf, TScalar, TColumn>
    where TColumn : struct, IVec<TColumn, TScalar>
{
    /// <summary>Gets the identity matrix.</summary>
    static abstract TSelf Identity { get; }

    /// <summary>Inverts a matrix.</summary>
    static abstract TSelf Invert(TSelf value);

    /// <summary>Returns the transposed inverse of a matrix.</summary>
    static abstract TSelf InverseTranspose(TSelf value);

    /// <summary>Returns the matrix adjugate.</summary>
    static abstract TSelf Adjugate(TSelf value);

    /// <summary>Returns whether every row and column has unit length within epsilon.</summary>
    static abstract bool IsNormalized(TSelf value, TScalar epsilon);

    /// <summary>Returns whether every row and column is normalized and mutually perpendicular within epsilon.</summary>
    static abstract bool IsOrthogonal(TSelf value, TScalar epsilon);

    /// <summary>Attempts to invert a matrix.</summary>
    static abstract bool TryInvert(TSelf value, out TSelf result);

    /// <summary>Gets the matrix determinant.</summary>
    TScalar Determinant { get; }

    /// <summary>Gets the matrix trace.</summary>
    TScalar Trace { get; }

    /// <summary>Gets the inverted matrix.</summary>
    TSelf Inverted { get; }

    /// <summary>Gets the transposed inverse matrix.</summary>
    TSelf InverseTransposed { get; }

    /// <summary>Returns whether every row and column has unit length within epsilon.</summary>
    bool IsNormalized(TScalar epsilon);

    /// <summary>Returns whether every row and column is normalized and mutually perpendicular within epsilon.</summary>
    bool IsOrthogonal(TScalar epsilon);
}
