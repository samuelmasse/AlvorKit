// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>{{TypeSummary}}</summary>
/// <remarks>A negative radius represents an empty sphere.</remarks>
/// <param name="center">The sphere center point.</param>
/// <param name="radius">The sphere radius. A negative radius represents an empty sphere.</param>
[StructLayout(LayoutKind.Sequential)]
[DebuggerDisplay("{ToString(),nq}")]
[ExcludeFromCodeCoverage]
public struct {{TypeName}}({{Vector3Type}} center, {{ScalarType}} radius)
    : {{ImplementedInterfaces}}
{
    /// <summary>The number of scalar components in a {{TypeName}}.</summary>
    public const int ComponentCount = 4;

    /// <summary>The byte size of a {{TypeName}}.</summary>
    public const int SizeInBytes = {{SizeBytes}};

    /// <summary>The sphere center point.</summary>
    public {{Vector3Type}} Center = center;

    /// <summary>The sphere radius. A negative radius represents an empty sphere.</summary>
    public {{ScalarType}} Radius = radius;

    /// <summary>Initializes components from the first <see cref="ComponentCount" /> values in a span.</summary>
    public {{TypeName}}(ReadOnlySpan<{{ScalarType}}> values)
        : this(
            new {{Vector3Type}}(
                ComponentFromSpan(values, 0),
                ComponentFromSpan(values, 1),
                ComponentFromSpan(values, 2)),
            ComponentFromSpan(values, 3))
    {
    }

    /// <summary>Gets an empty sphere represented by a negative radius.</summary>
    public static {{TypeName}} Empty => new(default, -{{OneLiteral}});

    /// <summary>Gets the number of scalar components.</summary>
    static int ISphere3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.ComponentCount =>
        ComponentCount;

    /// <summary>Gets the byte size.</summary>
    static int ISphere3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.SizeInBytes =>
        SizeInBytes;

    /// <summary>Gets or sets the center through the generic sphere interface.</summary>
    {{Vector3Type}} ISphere3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.Center
    {
        readonly get => Center;
        set => Center = value;
    }

    /// <summary>Gets or sets the radius through the generic sphere interface.</summary>
    {{ScalarType}} ISphere3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.Radius
    {
        readonly get => Radius;
        set => Radius = value;
    }

    /// <summary>Gets the sphere diameter.</summary>
    public readonly {{ScalarType}} Diameter => Radius * {{TwoLiteral}};

    /// <summary>Gets the squared radius.</summary>
    public readonly {{ScalarType}} RadiusSquared => Radius * Radius;

    /// <summary>Gets whether the radius is negative.</summary>
    public readonly bool IsEmpty => Radius < {{ZeroLiteral}};

    /// <summary>Creates a sphere from a center and radius. A negative radius represents an empty sphere.</summary>
    public static {{TypeName}} Create({{Vector3Type}} center, {{ScalarType}} radius) =>
        new(center, radius);

    /// <summary>Creates a sphere from the first <see cref="ComponentCount" /> values in a span.</summary>
    public static {{TypeName}} Create(ReadOnlySpan<{{ScalarType}}> values) =>
        new(values);

    /// <summary>Creates the smallest sphere centered on <paramref name="box" /> that contains its corners.</summary>
    public static {{TypeName}} CreateFromBox({{Box3Type}} box) =>
        box.IsEmpty ? Empty : new {{TypeName}}(box.Center, box.HalfSize.Length);

    /// <summary>Gets or sets a scalar component by zero-based index, with center components first.</summary>
    public {{ScalarType}} this[int index]
    {
        readonly get
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            return index < 3 ? Center[index] : Radius;
        }
        set
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            if (index < 3)
                Center[index] = value;
            else
                Radius = value;
        }
    }

    /// <summary>Gets a mutable reference to a scalar component by zero-based index.</summary>
    public static ref {{ScalarType}} ComponentRef(ref {{TypeName}} value, int index)
    {
        if ((uint)index >= ComponentCount)
            MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

        if (index < 3)
            return ref {{Vector3Type}}.ComponentRef(ref value.Center, index);

        return ref value.Radius;
    }

    /// <summary>Deconstructs the sphere into center and radius components.</summary>
    public readonly void Deconstruct(out {{Vector3Type}} center, out {{ScalarType}} radius)
    {
        center = Center;
        radius = Radius;
    }

    /// <summary>Copies the center components followed by radius into a caller-owned span.</summary>
    public readonly void CopyTo(Span<{{ScalarType}}> destination)
    {
        if (!TryCopyTo(destination))
            MathsThrowHelper.ThrowDestinationSpanTooShort(ComponentCount);
    }

    /// <summary>Attempts to copy the center components followed by radius into a caller-owned span.</summary>
    public readonly bool TryCopyTo(Span<{{ScalarType}}> destination)
    {
        if (destination.Length < ComponentCount)
            return false;

        Center.CopyTo(destination);
        destination[3] = Radius;
        return true;
    }

    /// <summary>Reads one constructor component after validating span length.</summary>
    private static {{ScalarType}} ComponentFromSpan(ReadOnlySpan<{{ScalarType}}> values, int index)
    {
        if (values.Length < ComponentCount)
            MathsThrowHelper.ThrowSourceSpanTooShort(ComponentCount);

        return values[index];
    }

    /// <summary>Returns whether this sphere contains <paramref name="point" />.</summary>
    public readonly bool Contains({{Vector3Type}} point) =>
        !IsEmpty && {{Vector3Type}}.DistanceSquared(Center, point) <= RadiusSquared;

    /// <summary>Returns whether this sphere fully contains <paramref name="sphere" />.</summary>
    public readonly bool Contains({{TypeName}} sphere)
    {
        if (IsEmpty || sphere.IsEmpty || Radius < sphere.Radius)
            return false;

        var remainingRadius = Radius - sphere.Radius;
        return {{Vector3Type}}.DistanceSquared(Center, sphere.Center) <= remainingRadius * remainingRadius;
    }

    /// <summary>Returns whether this sphere intersects <paramref name="sphere" />.</summary>
    public readonly bool Intersects({{TypeName}} sphere) =>
        Intersects(this, sphere);

    /// <summary>Returns whether two spheres intersect, counting touching surfaces as an intersection.</summary>
    public static bool Intersects({{TypeName}} left, {{TypeName}} right)
    {
        if (left.IsEmpty || right.IsEmpty)
            return false;

        var radiusSum = left.Radius + right.Radius;
        return {{Vector3Type}}.DistanceSquared(left.Center, right.Center) <= radiusSum * radiusSum;
    }

    /// <summary>Returns the closest point in this sphere to <paramref name="point" />.</summary>
    public readonly {{Vector3Type}} ClosestPoint({{Vector3Type}} point)
    {
        if (IsEmpty)
            return Center;

        var offset = point - Center;
        var distanceSquared = offset.LengthSquared;
        if (distanceSquared <= RadiusSquared || distanceSquared == {{ZeroLiteral}})
            return point;

        return Center + (offset * (Radius / ScalarMath.Sqrt(distanceSquared)));
    }

    /// <summary>Returns the distance from this sphere to <paramref name="point" />.</summary>
    public readonly {{ScalarType}} DistanceTo({{Vector3Type}} point)
    {
        if (IsEmpty)
            return {{ScalarType}}.PositiveInfinity;

        var distance = {{Vector3Type}}.Distance(Center, point) - Radius;
        return distance <= {{ZeroLiteral}} ? {{ZeroLiteral}} : distance;
    }

    /// <summary>Returns the squared distance from this sphere to <paramref name="point" />.</summary>
    public readonly {{ScalarType}} DistanceSquaredTo({{Vector3Type}} point)
    {
        if (IsEmpty)
            return {{ScalarType}}.PositiveInfinity;

        var centerDistanceSquared = {{Vector3Type}}.DistanceSquared(Center, point);
        if (centerDistanceSquared <= RadiusSquared)
            return {{ZeroLiteral}};

        var distance = ScalarMath.Sqrt(centerDistanceSquared) - Radius;
        return distance * distance;
    }

{{CrossScalarConversions}}
    /// <summary>Creates a sphere from a center/radius tuple.</summary>
    public static implicit operator {{TypeName}}(({{Vector3Type}} Center, {{ScalarType}} Radius) value) =>
        new(value.Center, value.Radius);

    /// <summary>Returns this sphere as a center/radius tuple.</summary>
    public static implicit operator ({{Vector3Type}} Center, {{ScalarType}} Radius)({{TypeName}} value) =>
        (value.Center, value.Radius);

    /// <summary>Returns whether two spheres have the same center and radius.</summary>
    public static bool operator ==({{TypeName}} left, {{TypeName}} right) =>
        left.Equals(right);

    /// <summary>Returns whether two spheres have a different center or radius.</summary>
    public static bool operator !=({{TypeName}} left, {{TypeName}} right) =>
        !left.Equals(right);

    /// <summary>Returns a string representation of this sphere.</summary>
    public override readonly string ToString() =>
        ToString(null, null);

    /// <summary>Returns a formatted string representation of this sphere.</summary>
    public readonly string ToString(string? format) =>
        ToString(format, null);

    /// <summary>Returns a culture-aware string representation of this sphere.</summary>
    public readonly string ToString(IFormatProvider? formatProvider) =>
        ToString(null, formatProvider);

    /// <summary>Returns a formatted, culture-aware string representation of this sphere.</summary>
    public readonly string ToString(string? format, IFormatProvider? formatProvider)
    {
        Span<char> destination = stackalloc char[512];
        return TryFormat(destination, out var charsWritten, format.AsSpan(), formatProvider)
            ? new string(destination[..charsWritten])
            : MathsFormatHelper.ToStringSlow(this, format, formatProvider, 1024);
    }

    /// <summary>Attempts to format this sphere into a caller-owned character span.</summary>
    public readonly bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        charsWritten = 0;
        var remainder = destination;

        if (!MathsFormatHelper.TryAppend("(".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Center, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(", ".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Radius, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(")".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);

        return true;
    }

    /// <summary>Attempts to format this sphere into a caller-owned UTF-8 byte span.</summary>
    public readonly bool TryFormat(
        Span<byte> utf8Destination,
        out int bytesWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        bytesWritten = 0;
        var remainder = utf8Destination;

        if (!MathsFormatHelper.TryAppend("("u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Center, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(", "u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Radius, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(")"u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);

        return true;
    }

    /// <summary>Compares this sphere to another sphere by center, then radius.</summary>
    public readonly int CompareTo({{TypeName}} other)
    {
        var centerComparison = Center.CompareTo(other.Center);
        if (centerComparison != 0)
            return centerComparison;

        return Radius.CompareTo(other.Radius);
    }

    /// <summary>Parses a sphere from a string.</summary>
    public static {{TypeName}} Parse(string s, IFormatProvider? formatProvider)
    {
        ArgumentNullException.ThrowIfNull(s);
        return Parse(s.AsSpan(), formatProvider);
    }

    /// <summary>Attempts to parse a sphere from a string.</summary>
    public static bool TryParse(string? s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (s is null)
        {
            result = default;
            return false;
        }

        return TryParse(s.AsSpan(), formatProvider, out result);
    }

    /// <summary>Parses a sphere from a character span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<char> s, IFormatProvider? formatProvider) =>
        TryParse(s, formatProvider, out var result)
            ? result
            : throw new FormatException("Input sphere text was not in a correct format.");

    /// <summary>Attempts to parse a sphere from a character span.</summary>
    public static bool TryParse(ReadOnlySpan<char> s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(s, out var body) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} center) ||
            !MathsParseHelper.TryParseComponent(body.Trim(), formatProvider, out {{ScalarType}} radius))
        {
            return false;
        }

        result = new(center, radius);
        return true;
    }

    /// <summary>Parses a sphere from a UTF-8 byte span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider) =>
        TryParse(utf8Text, formatProvider, out var result)
            ? result
            : throw new FormatException("Input sphere text was not in a correct format.");

    /// <summary>Attempts to parse a sphere from a UTF-8 byte span.</summary>
    public static bool TryParse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(utf8Text, out var body) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} center) ||
            !MathsParseHelper.TryParseComponent(
                MathsUtf8TextHelper.TrimAsciiWhitespace(body),
                formatProvider,
                out {{ScalarType}} radius))
        {
            return false;
        }

        result = new(center, radius);
        return true;
    }

    /// <summary>Returns whether this sphere has the same center and radius as another sphere.</summary>
    public readonly bool Equals({{TypeName}} other) =>
        Center.Equals(other.Center) && Radius.Equals(other.Radius);

    /// <summary>Returns whether this sphere has the same center and radius as an object.</summary>
    public override readonly bool Equals(object? obj) =>
        obj is {{TypeName}} other && Equals(other);

    /// <summary>Returns a hash code for this sphere.</summary>
    public override readonly int GetHashCode() =>
        HashCode.Combine(Center, Radius);
}
