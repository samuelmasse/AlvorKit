// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>{{TypeSummary}}</summary>
/// <param name="normal">The plane normal.</param>
/// <param name="offset">The offset in <c>dot(Normal, point) + Offset</c> form.</param>
[StructLayout(LayoutKind.Sequential)]
[DebuggerDisplay("{ToString(),nq}")]
[ExcludeFromCodeCoverage]
public struct {{TypeName}}({{Vector3Type}} normal, {{ScalarType}} offset)
    : {{ImplementedInterfaces}}
{
    /// <summary>The number of scalar components in a {{TypeName}}.</summary>
    public const int ComponentCount = 4;

    /// <summary>The byte size of a {{TypeName}}.</summary>
    public const int SizeInBytes = {{SizeBytes}};

    /// <summary>The plane normal.</summary>
    public {{Vector3Type}} Normal = normal;

    /// <summary>The offset in <c>dot(Normal, point) + Offset</c> form.</summary>
    public {{ScalarType}} Offset = offset;

    /// <summary>Creates a plane from equation coefficients.</summary>
    public {{TypeName}}({{Vector4Type}} coefficients)
        : this(new {{Vector3Type}}(coefficients.X, coefficients.Y, coefficients.Z), coefficients.W)
    {
    }

    /// <summary>Initializes components from the first <see cref="ComponentCount" /> values in a span.</summary>
    public {{TypeName}}(ReadOnlySpan<{{ScalarType}}> values)
        : this(
            new {{Vector3Type}}(
                ComponentFromSpan(values, 0),
                ComponentFromSpan(values, 1),
                ComponentFromSpan(values, 2)),
            ComponentFromSpan(values, 3))
    {
    }

    /// <summary>Gets the degenerate zero plane.</summary>
    public static {{TypeName}} Zero => default;

    /// <summary>Gets the number of scalar components.</summary>
    static int IPlane3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}>.ComponentCount =>
        ComponentCount;

    /// <summary>Gets the byte size.</summary>
    static int IPlane3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}>.SizeInBytes =>
        SizeInBytes;

    /// <summary>Gets or sets the normal through the generic plane interface.</summary>
    {{Vector3Type}} IPlane3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}>.Normal
    {
        readonly get => Normal;
        set => Normal = value;
    }

    /// <summary>Gets or sets the offset through the generic plane interface.</summary>
    {{ScalarType}} IPlane3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}>.Offset
    {
        readonly get => Offset;
        set => Offset = value;
    }

    /// <summary>Gets or sets the plane equation coefficients as <c>(Normal.X, Normal.Y, Normal.Z, Offset)</c>.</summary>
    public {{Vector4Type}} Coefficients
    {
        readonly get => new(Normal, Offset);
        set
        {
            Normal = new {{Vector3Type}}(value.X, value.Y, value.Z);
            Offset = value.W;
        }
    }

    /// <summary>Gets the squared normal length.</summary>
    public readonly {{ScalarType}} NormalLengthSquared => Normal.LengthSquared;

    /// <summary>Gets the normal length.</summary>
    public readonly {{ScalarType}} NormalLength => Normal.Length;

    /// <summary>Gets this plane divided by its normal length.</summary>
    public readonly {{TypeName}} Normalized => Normalize(this);

    /// <summary>Returns this plane divided by its normal length, or fallback when its normal is zero.</summary>
    public readonly {{TypeName}} NormalizedOr({{TypeName}} fallback) =>
        TryNormalize(out var result) ? result : fallback;

    /// <summary>Returns this plane divided by its normal length when possible.</summary>
    public readonly bool TryNormalize(out {{TypeName}} result) =>
        TryNormalize(this, out result);

    /// <summary>Gets the plane with the opposite normal and offset.</summary>
    public readonly {{TypeName}} Flipped => Flip(this);

    /// <summary>Creates a plane from a normal and offset.</summary>
    public static {{TypeName}} Create({{Vector3Type}} normal, {{ScalarType}} offset) =>
        new(normal, offset);

    /// <summary>Creates a plane from equation coefficients.</summary>
    public static {{TypeName}} Create({{Vector4Type}} coefficients) =>
        new(coefficients);

    /// <summary>Creates a plane from the first <see cref="ComponentCount" /> values in a span.</summary>
    public static {{TypeName}} Create(ReadOnlySpan<{{ScalarType}}> values) =>
        new(values);

    /// <summary>Creates a plane that contains <paramref name="point" /> with the supplied normal.</summary>
    public static {{TypeName}} CreateFromPointNormal({{Vector3Type}} point, {{Vector3Type}} normal) =>
        new(normal, -{{Vector3Type}}.Dot(normal, point));

    /// <summary>Creates a normalized plane that contains three non-collinear points.</summary>
    public static {{TypeName}} CreateFromPoints({{Vector3Type}} point0, {{Vector3Type}} point1, {{Vector3Type}} point2) =>
        TryCreateFromPoints(point0, point1, point2, out var result)
            ? result
            : throw new InvalidOperationException("Points are collinear and cannot define a plane.");

    /// <summary>Attempts to create a normalized plane that contains three non-collinear points.</summary>
    public static bool TryCreateFromPoints(
        {{Vector3Type}} point0,
        {{Vector3Type}} point1,
        {{Vector3Type}} point2,
        out {{TypeName}} result)
    {
        var normal = {{Vector3Type}}.Cross(point1 - point0, point2 - point0);
        var lengthSquared = normal.LengthSquared;
        if (lengthSquared <= {{ZeroLiteral}})
        {
            result = default;
            return false;
        }

        normal /= ScalarMath.Sqrt(lengthSquared);
        result = CreateFromPointNormal(point0, normal);
        return true;
    }

    /// <summary>Gets or sets a component by zero-based index.</summary>
    public {{ScalarType}} this[int index]
    {
        readonly get
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            return index < 3 ? Normal[index] : Offset;
        }
        set
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            if (index < 3)
                Normal[index] = value;
            else
                Offset = value;
        }
    }

    /// <summary>Gets a mutable reference to a component by zero-based index.</summary>
    public static ref {{ScalarType}} ComponentRef(ref {{TypeName}} value, int index)
    {
        if ((uint)index >= ComponentCount)
            MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

        if (index < 3)
            return ref {{Vector3Type}}.ComponentRef(ref value.Normal, index);

        return ref value.Offset;
    }

    /// <summary>Deconstructs the plane into normal and offset components.</summary>
    public readonly void Deconstruct(out {{Vector3Type}} normal, out {{ScalarType}} offset)
    {
        normal = Normal;
        offset = Offset;
    }

    /// <summary>Copies this plane into an array.</summary>
    public readonly void CopyTo({{ScalarType}}[] array)
    {
        ArgumentNullException.ThrowIfNull(array);
        CopyTo(array.AsSpan());
    }

    /// <summary>Copies this plane into an array starting at <paramref name="index" />.</summary>
    public readonly void CopyTo({{ScalarType}}[] array, int index)
    {
        ArgumentNullException.ThrowIfNull(array);
        CopyTo(array.AsSpan(index));
    }

    /// <summary>Copies this plane into a destination span.</summary>
    public readonly void CopyTo(Span<{{ScalarType}}> destination)
    {
        if (!TryCopyTo(destination))
            MathsThrowHelper.ThrowDestinationSpanTooShort(ComponentCount);
    }

    /// <summary>Attempts to copy this plane into a destination span.</summary>
    public readonly bool TryCopyTo(Span<{{ScalarType}}> destination)
    {
        if (destination.Length < ComponentCount)
            return false;

        destination[0] = Normal.X;
        destination[1] = Normal.Y;
        destination[2] = Normal.Z;
        destination[3] = Offset;
        return true;
    }

    /// <summary>Reads one constructor component after validating span length.</summary>
    private static {{ScalarType}} ComponentFromSpan(ReadOnlySpan<{{ScalarType}}> values, int index)
    {
        if (values.Length < ComponentCount)
            MathsThrowHelper.ThrowSourceSpanTooShort(ComponentCount);

        return values[index];
    }

    /// <summary>Evaluates <c>dot(Normal, point) + Offset</c>.</summary>
    public readonly {{ScalarType}} Evaluate({{Vector3Type}} point) =>
        Evaluate(this, point);

    /// <summary>Classifies the side containing <paramref name="point" /> from the exact sign of <see cref="Evaluate({{Vector3Type}})" />.</summary>
    public readonly PlaneIntersectionKind Classify({{Vector3Type}} point) =>
        Classify(this, point);

    /// <summary>Returns the dot product of the plane coefficients and a four-component vector.</summary>
    public readonly {{ScalarType}} Dot({{Vector4Type}} value) =>
        Dot(this, value);

    /// <summary>Returns the dot product of the plane normal and a vector.</summary>
    public readonly {{ScalarType}} DotNormal({{Vector3Type}} value) =>
        DotNormal(this, value);

    /// <summary>Returns the signed distance from the plane to a point.</summary>
    public readonly {{ScalarType}} SignedDistanceTo({{Vector3Type}} point) =>
        Evaluate(point) / NormalLength;

    /// <summary>Returns the absolute distance from the plane to a point.</summary>
    public readonly {{ScalarType}} DistanceTo({{Vector3Type}} point) =>
        ScalarMath.Abs(SignedDistanceTo(point));

    /// <summary>Returns the closest point on this plane to <paramref name="point" />.</summary>
    public readonly {{Vector3Type}} ClosestPoint({{Vector3Type}} point) =>
        ProjectPoint(point);

    /// <summary>Returns the projection of <paramref name="point" /> onto this plane.</summary>
    public readonly {{Vector3Type}} ProjectPoint({{Vector3Type}} point) =>
        point - (Normal * (Evaluate(point) / NormalLengthSquared));

    /// <summary>Returns <paramref name="point" /> reflected across this plane.</summary>
    public readonly {{Vector3Type}} ReflectPoint({{Vector3Type}} point) =>
        point - (Normal * (({{TwoLiteral}} * Evaluate(point)) / NormalLengthSquared));

    /// <summary>
    /// Classifies <paramref name="box" /> against this plane. An empty box returns <see cref="PlaneIntersectionKind.Intersecting" />.
    /// </summary>
    public readonly PlaneIntersectionKind Classify({{Box3Type}} box)
    {
        if (box.IsEmpty)
            return PlaneIntersectionKind.Intersecting;

        var minimum = Evaluate(NegativeVertex(box));
        var maximum = Evaluate(PositiveVertex(box));
        if (minimum > {{ZeroLiteral}})
            return PlaneIntersectionKind.Positive;
        if (maximum < {{ZeroLiteral}})
            return PlaneIntersectionKind.Negative;

        return PlaneIntersectionKind.Intersecting;
    }

    /// <summary>
    /// Classifies <paramref name="sphere" /> against this plane. An empty sphere returns <see cref="PlaneIntersectionKind.Intersecting" />.
    /// </summary>
    public readonly PlaneIntersectionKind Classify({{Sphere3Type}} sphere)
    {
        if (sphere.IsEmpty)
            return PlaneIntersectionKind.Intersecting;

        var centerDistance = Evaluate(sphere.Center);
        var radiusDistance = sphere.Radius * NormalLength;
        if (centerDistance > radiusDistance)
            return PlaneIntersectionKind.Positive;
        if (centerDistance < -radiusDistance)
            return PlaneIntersectionKind.Negative;

        return PlaneIntersectionKind.Intersecting;
    }

    /// <summary>Returns value divided by its normal length.</summary>
    public static {{TypeName}} Normalize({{TypeName}} value)
    {
        var length = value.NormalLength;
        return new(value.Normal / length, value.Offset / length);
    }

    /// <summary>Returns value divided by its normal length when possible.</summary>
    public static bool TryNormalize({{TypeName}} value, out {{TypeName}} result)
    {
        var lengthSquared = value.NormalLengthSquared;
        if (lengthSquared <= {{ZeroLiteral}})
        {
            result = default;
            return false;
        }

        var length = ScalarMath.Sqrt(lengthSquared);
        result = new(value.Normal / length, value.Offset / length);
        return true;
    }

    /// <summary>Returns value with the opposite normal and offset.</summary>
    public static {{TypeName}} Flip({{TypeName}} value) =>
        new(-value.Normal, -value.Offset);

    /// <summary>Evaluates <c>dot(plane.Normal, point) + plane.Offset</c>.</summary>
    public static {{ScalarType}} Evaluate({{TypeName}} plane, {{Vector3Type}} point) =>
        {{Vector3Type}}.Dot(plane.Normal, point) + plane.Offset;

    /// <summary>Classifies the side containing <paramref name="point" /> from the exact sign of <see cref="Evaluate({{TypeName}}, {{Vector3Type}})" />.</summary>
    public static PlaneIntersectionKind Classify({{TypeName}} plane, {{Vector3Type}} point) =>
        ClassifyEvaluation(Evaluate(plane, point));

    /// <summary>Returns the dot product of the plane coefficients and a four-component vector.</summary>
    public static {{ScalarType}} Dot({{TypeName}} plane, {{Vector4Type}} value) =>
        (plane.Normal.X * value.X) + (plane.Normal.Y * value.Y) + (plane.Normal.Z * value.Z) + (plane.Offset * value.W);

    /// <summary>Returns the dot product of the plane normal and a vector.</summary>
    public static {{ScalarType}} DotNormal({{TypeName}} plane, {{Vector3Type}} value) =>
        {{Vector3Type}}.Dot(plane.Normal, value);

    /// <summary>Returns whether value has a unit-length normal.</summary>
    public static bool IsNormalized({{TypeName}} value, {{ScalarType}} epsilon) =>
        ScalarMath.Abs(value.NormalLengthSquared - {{OneLiteral}}) <= epsilon;

    /// <summary>Returns whether this plane has a unit-length normal.</summary>
    public readonly bool IsNormalized({{ScalarType}} epsilon) =>
        IsNormalized(this, epsilon);

    /// <summary>Transforms a plane by a matrix using inverse-transpose plane semantics.</summary>
    public static {{TypeName}} Transform({{TypeName}} plane, {{Matrix4Type}} matrix) =>
        TryTransform(plane, matrix, out var result)
            ? result
            : throw new InvalidOperationException("Matrix is singular and cannot transform a plane.");

    /// <summary>Attempts to transform a plane by an invertible matrix.</summary>
    public static bool TryTransform({{TypeName}} plane, {{Matrix4Type}} matrix, out {{TypeName}} result)
    {
        if (!{{Matrix4Type}}.TryInvert(matrix, out var inverse))
        {
            result = default;
            return false;
        }

        result = new {{TypeName}}(inverse.Transposed * plane.Coefficients);
        return true;
    }

    /// <summary>Transforms a plane normal by a quaternion while preserving its offset.</summary>
    public static {{TypeName}} Transform({{TypeName}} plane, {{QuaternionType}} rotation) =>
        new(rotation * plane.Normal, plane.Offset);

    /// <summary>Returns this plane unchanged.</summary>
    public static {{TypeName}} operator +({{TypeName}} value) =>
        value;

    /// <summary>Returns the plane with the opposite normal and offset.</summary>
    public static {{TypeName}} operator -({{TypeName}} value) =>
        Flip(value);

{{CrossScalarConversions}}{{SystemNumericsConversions}}
    /// <summary>Creates a plane from a normal/offset tuple.</summary>
    public static implicit operator {{TypeName}}(({{Vector3Type}} Normal, {{ScalarType}} Offset) value) =>
        new(value.Normal, value.Offset);

    /// <summary>Returns this plane as a normal/offset tuple.</summary>
    public static implicit operator ({{Vector3Type}} Normal, {{ScalarType}} Offset)({{TypeName}} value) =>
        (value.Normal, value.Offset);

    /// <summary>Returns whether two planes have identical coefficients.</summary>
    public static bool operator ==({{TypeName}} left, {{TypeName}} right) =>
        left.Equals(right);

    /// <summary>Returns whether two planes have any different coefficient.</summary>
    public static bool operator !=({{TypeName}} left, {{TypeName}} right) =>
        !left.Equals(right);

    /// <summary>Returns a string representation of this plane.</summary>
    public override readonly string ToString() =>
        Coefficients.ToString();

    /// <summary>Returns a formatted string representation of this plane.</summary>
    public readonly string ToString(string? format) =>
        Coefficients.ToString(format);

    /// <summary>Returns a culture-aware string representation of this plane.</summary>
    public readonly string ToString(IFormatProvider? formatProvider) =>
        Coefficients.ToString(formatProvider);

    /// <summary>Returns a formatted, culture-aware string representation of this plane.</summary>
    public readonly string ToString(string? format, IFormatProvider? formatProvider) =>
        Coefficients.ToString(format, formatProvider);

    /// <summary>Attempts to format this plane into a caller-owned character span.</summary>
    public readonly bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider) =>
        Coefficients.TryFormat(destination, out charsWritten, format, formatProvider);

    /// <summary>Attempts to format this plane into a caller-owned UTF-8 byte span.</summary>
    public readonly bool TryFormat(
        Span<byte> utf8Destination,
        out int bytesWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider) =>
        Coefficients.TryFormat(utf8Destination, out bytesWritten, format, formatProvider);

    /// <summary>Compares this plane to another plane by coefficient order.</summary>
    public readonly int CompareTo({{TypeName}} other) =>
        Coefficients.CompareTo(other.Coefficients);

    /// <summary>Parses a plane from a string.</summary>
    public static {{TypeName}} Parse(string s, IFormatProvider? formatProvider)
    {
        ArgumentNullException.ThrowIfNull(s);
        return Parse(s.AsSpan(), formatProvider);
    }

    /// <summary>Attempts to parse a plane from a string.</summary>
    public static bool TryParse(string? s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (s is null)
        {
            result = default;
            return false;
        }

        return TryParse(s.AsSpan(), formatProvider, out result);
    }

    /// <summary>Parses a plane from a character span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<char> s, IFormatProvider? formatProvider) =>
        TryParse(s, formatProvider, out var result)
            ? result
            : throw new FormatException("Input plane text was not in a correct format.");

    /// <summary>Attempts to parse a plane from a character span.</summary>
    public static bool TryParse(ReadOnlySpan<char> s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (!{{Vector4Type}}.TryParse(s, formatProvider, out var coefficients))
        {
            result = default;
            return false;
        }

        result = new(coefficients);
        return true;
    }

    /// <summary>Parses a plane from a UTF-8 byte span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider) =>
        TryParse(utf8Text, formatProvider, out var result)
            ? result
            : throw new FormatException("Input plane text was not in a correct format.");

    /// <summary>Attempts to parse a plane from a UTF-8 byte span.</summary>
    public static bool TryParse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (!{{Vector4Type}}.TryParse(utf8Text, formatProvider, out var coefficients))
        {
            result = default;
            return false;
        }

        result = new(coefficients);
        return true;
    }

    /// <summary>Returns whether this plane has the same coefficients as another plane.</summary>
    public readonly bool Equals({{TypeName}} other) =>
        Normal.Equals(other.Normal) && Offset.Equals(other.Offset);

    /// <summary>Returns whether this plane has the same coefficients as an object.</summary>
    public override readonly bool Equals(object? obj) =>
        obj is {{TypeName}} other && Equals(other);

    /// <summary>Classifies an evaluated plane equation value by its exact sign.</summary>
    private static PlaneIntersectionKind ClassifyEvaluation({{ScalarType}} value)
    {
        if (value < {{ZeroLiteral}})
            return PlaneIntersectionKind.Negative;
        if (value > {{ZeroLiteral}})
            return PlaneIntersectionKind.Positive;

        return PlaneIntersectionKind.Intersecting;
    }

    /// <summary>Returns the box vertex farthest along this plane normal.</summary>
    private readonly {{Vector3Type}} PositiveVertex({{Box3Type}} box) =>
        new(
            Normal.X >= {{ZeroLiteral}} ? box.Max.X : box.Min.X,
            Normal.Y >= {{ZeroLiteral}} ? box.Max.Y : box.Min.Y,
            Normal.Z >= {{ZeroLiteral}} ? box.Max.Z : box.Min.Z);

    /// <summary>Returns the box vertex farthest against this plane normal.</summary>
    private readonly {{Vector3Type}} NegativeVertex({{Box3Type}} box) =>
        new(
            Normal.X >= {{ZeroLiteral}} ? box.Min.X : box.Max.X,
            Normal.Y >= {{ZeroLiteral}} ? box.Min.Y : box.Max.Y,
            Normal.Z >= {{ZeroLiteral}} ? box.Min.Z : box.Max.Z);

    /// <summary>Returns a hash code for this plane.</summary>
    public override readonly int GetHashCode() =>
        HashCode.Combine(Normal, Offset);
}
