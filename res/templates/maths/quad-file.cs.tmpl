// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>{{TypeSummary}}</summary>
/// <param name="topLeft">The top-left corner.</param>
/// <param name="topRight">The top-right corner.</param>
/// <param name="bottomLeft">The bottom-left corner.</param>
/// <param name="bottomRight">The bottom-right corner.</param>
[StructLayout(LayoutKind.Sequential)]
[DebuggerDisplay("{ToString(),nq}")]
[ExcludeFromCodeCoverage]
public struct {{TypeName}}(
    {{Vector3Type}} topLeft,
    {{Vector3Type}} topRight,
    {{Vector3Type}} bottomLeft,
    {{Vector3Type}} bottomRight)
    : {{ImplementedInterfaces}}
{
    /// <summary>The number of scalar components in a {{TypeName}}.</summary>
    public const int ComponentCount = 12;

    /// <summary>The byte size of a {{TypeName}}.</summary>
    public const int SizeInBytes = {{SizeBytes}};

    /// <summary>The top-left corner.</summary>
    public {{Vector3Type}} TopLeft = topLeft;

    /// <summary>The top-right corner.</summary>
    public {{Vector3Type}} TopRight = topRight;

    /// <summary>The bottom-left corner.</summary>
    public {{Vector3Type}} BottomLeft = bottomLeft;

    /// <summary>The bottom-right corner.</summary>
    public {{Vector3Type}} BottomRight = bottomRight;

    /// <summary>Gets the number of scalar components.</summary>
    static int IQuad3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.ComponentCount => ComponentCount;

    /// <summary>Gets the byte size.</summary>
    static int IQuad3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.SizeInBytes => SizeInBytes;

    /// <summary>Gets or sets the top-left corner through the generic quad interface.</summary>
    {{Vector3Type}} IQuad3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.TopLeft
    {
        readonly get => TopLeft;
        set => TopLeft = value;
    }

    /// <summary>Gets or sets the top-right corner through the generic quad interface.</summary>
    {{Vector3Type}} IQuad3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.TopRight
    {
        readonly get => TopRight;
        set => TopRight = value;
    }

    /// <summary>Gets or sets the bottom-left corner through the generic quad interface.</summary>
    {{Vector3Type}} IQuad3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.BottomLeft
    {
        readonly get => BottomLeft;
        set => BottomLeft = value;
    }

    /// <summary>Gets or sets the bottom-right corner through the generic quad interface.</summary>
    {{Vector3Type}} IQuad3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Box3Type}}>.BottomRight
    {
        readonly get => BottomRight;
        set => BottomRight = value;
    }

    /// <summary>Initializes corners from the first <see cref="ComponentCount" /> values in a span.</summary>
    public {{TypeName}}(ReadOnlySpan<{{ScalarType}}> values)
        : this(
            CreateVector(values, 0),
            CreateVector(values, 3),
            CreateVector(values, 6),
            CreateVector(values, 9))
    {
    }

    /// <summary>Gets the average of the four corners.</summary>
    public readonly {{Vector3Type}} Center =>
        (TopLeft + TopRight + BottomLeft + BottomRight) / {{FourLiteral}};

    /// <summary>Gets the axis-aligned box containing all four corners.</summary>
    public readonly {{Box3Type}} Bounds =>
        {{Box3Type}}.CreateFromCorners(TopLeft, TopRight).Including(BottomLeft).Including(BottomRight);

    /// <summary>Creates a quad from four corners.</summary>
    public static {{TypeName}} Create(
        {{Vector3Type}} topLeft,
        {{Vector3Type}} topRight,
        {{Vector3Type}} bottomLeft,
        {{Vector3Type}} bottomRight) =>
        new(topLeft, topRight, bottomLeft, bottomRight);

    /// <summary>Creates a quad from the first <see cref="ComponentCount" /> scalar values in a span.</summary>
    public static {{TypeName}} Create(ReadOnlySpan<{{ScalarType}}> values) =>
        new(values);

    /// <summary>Gets or sets a scalar component by zero-based index, with corners in top-left, top-right, bottom-left, bottom-right order.</summary>
    public {{ScalarType}} this[int index]
    {
        readonly get
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            if (index < 3)
                return TopLeft[index];
            if (index < 6)
                return TopRight[index - 3];
            return index < 9 ? BottomLeft[index - 6] : BottomRight[index - 9];
        }
        set
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            if (index < 3)
                TopLeft[index] = value;
            else if (index < 6)
                TopRight[index - 3] = value;
            else if (index < 9)
                BottomLeft[index - 6] = value;
            else
                BottomRight[index - 9] = value;
        }
    }

    /// <summary>Gets a mutable reference to a scalar component by zero-based index.</summary>
    public static ref {{ScalarType}} ComponentRef(ref {{TypeName}} value, int index)
    {
        if ((uint)index >= ComponentCount)
            MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

        if (index < 3)
            return ref {{Vector3Type}}.ComponentRef(ref value.TopLeft, index);
        if (index < 6)
            return ref {{Vector3Type}}.ComponentRef(ref value.TopRight, index - 3);
        if (index < 9)
            return ref {{Vector3Type}}.ComponentRef(ref value.BottomLeft, index - 6);

        return ref {{Vector3Type}}.ComponentRef(ref value.BottomRight, index - 9);
    }

    /// <summary>Deconstructs the quad into its four corners.</summary>
    public readonly void Deconstruct(
        out {{Vector3Type}} topLeft,
        out {{Vector3Type}} topRight,
        out {{Vector3Type}} bottomLeft,
        out {{Vector3Type}} bottomRight)
    {
        topLeft = TopLeft;
        topRight = TopRight;
        bottomLeft = BottomLeft;
        bottomRight = BottomRight;
    }

    /// <summary>Copies corner components into a caller-owned span in top-left, top-right, bottom-left, bottom-right order.</summary>
    public readonly void CopyTo(Span<{{ScalarType}}> destination)
    {
        if (!TryCopyTo(destination))
            MathsThrowHelper.ThrowDestinationSpanTooShort(ComponentCount);
    }

    /// <summary>Attempts to copy corner components into a caller-owned span in top-left, top-right, bottom-left, bottom-right order.</summary>
    public readonly bool TryCopyTo(Span<{{ScalarType}}> destination)
    {
        if (destination.Length < ComponentCount)
            return false;

        TopLeft.CopyTo(destination);
        TopRight.CopyTo(destination[3..]);
        BottomLeft.CopyTo(destination[6..]);
        BottomRight.CopyTo(destination[9..]);
        return true;
    }

{{CrossScalarConversions}}
    /// <summary>Creates a quad from a top-left/top-right/bottom-left/bottom-right tuple.</summary>
    public static implicit operator {{TypeName}}((
        {{Vector3Type}} TopLeft,
        {{Vector3Type}} TopRight,
        {{Vector3Type}} BottomLeft,
        {{Vector3Type}} BottomRight) value) =>
        new(value.TopLeft, value.TopRight, value.BottomLeft, value.BottomRight);

    /// <summary>Returns this quad as a top-left/top-right/bottom-left/bottom-right tuple.</summary>
    public static implicit operator (
        {{Vector3Type}} TopLeft,
        {{Vector3Type}} TopRight,
        {{Vector3Type}} BottomLeft,
        {{Vector3Type}} BottomRight)({{TypeName}} value) =>
        (value.TopLeft, value.TopRight, value.BottomLeft, value.BottomRight);

    /// <summary>Returns whether two quads have the same corners.</summary>
    public static bool operator ==({{TypeName}} left, {{TypeName}} right) =>
        left.Equals(right);

    /// <summary>Returns whether two quads have any different corner.</summary>
    public static bool operator !=({{TypeName}} left, {{TypeName}} right) =>
        !left.Equals(right);

    /// <summary>Returns a string representation of this quad.</summary>
    public override readonly string ToString() =>
        ToString(null, null);

    /// <summary>Returns a formatted string representation of this quad.</summary>
    public readonly string ToString(string? format) =>
        ToString(format, null);

    /// <summary>Returns a culture-aware string representation of this quad.</summary>
    public readonly string ToString(IFormatProvider? formatProvider) =>
        ToString(null, formatProvider);

    /// <summary>Returns a formatted, culture-aware string representation of this quad.</summary>
    public readonly string ToString(string? format, IFormatProvider? formatProvider)
    {
        Span<char> destination = stackalloc char[1024];
        return TryFormat(destination, out var charsWritten, format.AsSpan(), formatProvider)
            ? new string(destination[..charsWritten])
            : MathsFormatHelper.ToStringSlow(this, format, formatProvider, 2048);
    }

    /// <summary>Attempts to format this quad into a caller-owned character span.</summary>
    public readonly bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        charsWritten = 0;
        var remainder = destination;

        if (!MathsFormatHelper.TryAppend("(".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(TopLeft, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(", ".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(TopRight, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(", ".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(BottomLeft, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(", ".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(BottomRight, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(")".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);

        return true;
    }

    /// <summary>Attempts to format this quad into a caller-owned UTF-8 byte span.</summary>
    public readonly bool TryFormat(
        Span<byte> utf8Destination,
        out int bytesWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        bytesWritten = 0;
        var remainder = utf8Destination;

        if (!MathsFormatHelper.TryAppend("("u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(TopLeft, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(", "u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(TopRight, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(", "u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(BottomLeft, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(", "u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(BottomRight, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(")"u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);

        return true;
    }

    /// <summary>Compares this quad to another quad by top-left, top-right, bottom-left, then bottom-right.</summary>
    public readonly int CompareTo({{TypeName}} other)
    {
        var topLeftComparison = TopLeft.CompareTo(other.TopLeft);
        if (topLeftComparison != 0)
            return topLeftComparison;

        var topRightComparison = TopRight.CompareTo(other.TopRight);
        if (topRightComparison != 0)
            return topRightComparison;

        var bottomLeftComparison = BottomLeft.CompareTo(other.BottomLeft);
        if (bottomLeftComparison != 0)
            return bottomLeftComparison;

        return BottomRight.CompareTo(other.BottomRight);
    }

    /// <summary>Parses a quad from a string.</summary>
    public static {{TypeName}} Parse(string s, IFormatProvider? formatProvider)
    {
        ArgumentNullException.ThrowIfNull(s);
        return Parse(s.AsSpan(), formatProvider);
    }

    /// <summary>Attempts to parse a quad from a string.</summary>
    public static bool TryParse(string? s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (s is null)
        {
            result = default;
            return false;
        }

        return TryParse(s.AsSpan(), formatProvider, out result);
    }

    /// <summary>Parses a quad from a character span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<char> s, IFormatProvider? formatProvider) =>
        TryParse(s, formatProvider, out var result)
            ? result
            : throw new FormatException("Input quad text was not in a correct format.");

    /// <summary>Attempts to parse a quad from a character span.</summary>
    public static bool TryParse(ReadOnlySpan<char> s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(s, out var body) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} topLeft) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} topRight) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} bottomLeft) ||
            !MathsParseHelper.TryParseComponent(body.Trim(), formatProvider, out {{Vector3Type}} bottomRight))
        {
            return false;
        }

        result = new(topLeft, topRight, bottomLeft, bottomRight);
        return true;
    }

    /// <summary>Parses a quad from a UTF-8 byte span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider) =>
        TryParse(utf8Text, formatProvider, out var result)
            ? result
            : throw new FormatException("Input quad text was not in a correct format.");

    /// <summary>Attempts to parse a quad from a UTF-8 byte span.</summary>
    public static bool TryParse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(utf8Text, out var body) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} topLeft) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} topRight) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} bottomLeft) ||
            !MathsParseHelper.TryParseComponent(
                MathsUtf8TextHelper.TrimAsciiWhitespace(body),
                formatProvider,
                out {{Vector3Type}} bottomRight))
        {
            return false;
        }

        result = new(topLeft, topRight, bottomLeft, bottomRight);
        return true;
    }

    /// <summary>Returns whether this quad has the same corners as another quad.</summary>
    public readonly bool Equals({{TypeName}} other) =>
        TopLeft.Equals(other.TopLeft) &&
        TopRight.Equals(other.TopRight) &&
        BottomLeft.Equals(other.BottomLeft) &&
        BottomRight.Equals(other.BottomRight);

    /// <summary>Returns whether this quad has the same corners as an object.</summary>
    public override readonly bool Equals(object? obj) =>
        obj is {{TypeName}} other && Equals(other);

    /// <summary>Returns a hash code for this quad.</summary>
    public override readonly int GetHashCode() =>
        HashCode.Combine(TopLeft, TopRight, BottomLeft, BottomRight);

    /// <summary>Creates one vector after validating span length.</summary>
    private static {{Vector3Type}} CreateVector(ReadOnlySpan<{{ScalarType}}> values, int start)
    {
        if (values.Length < ComponentCount)
            MathsThrowHelper.ThrowSourceSpanTooShort(ComponentCount);

        return new {{Vector3Type}}(values[start..]);
    }
}
