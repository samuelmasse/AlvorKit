// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>{{TypeSummary}}</summary>
/// <param name="start">The segment start point.</param>
/// <param name="end">The segment end point.</param>
[StructLayout(LayoutKind.Sequential)]
[DebuggerDisplay("{ToString(),nq}")]
[ExcludeFromCodeCoverage]
public struct {{TypeName}}({{Vector3Type}} start, {{Vector3Type}} end)
    : {{ImplementedInterfaces}}
{
    /// <summary>The number of scalar components in a {{TypeName}}.</summary>
    public const int ComponentCount = 6;

    /// <summary>The byte size of a {{TypeName}}.</summary>
    public const int SizeInBytes = {{SizeBytes}};

    /// <summary>The segment start point.</summary>
    public {{Vector3Type}} Start = start;

    /// <summary>The segment end point.</summary>
    public {{Vector3Type}} End = end;

    /// <summary>Gets the number of scalar components.</summary>
    static int ISegment3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}},
        {{Box3Type}}, {{Sphere3Type}}>.ComponentCount => ComponentCount;

    /// <summary>Gets the byte size.</summary>
    static int ISegment3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}},
        {{Box3Type}}, {{Sphere3Type}}>.SizeInBytes => SizeInBytes;

    /// <summary>Gets or sets the start point through the generic segment interface.</summary>
    {{Vector3Type}} ISegment3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}},
        {{Box3Type}}, {{Sphere3Type}}>.Start
    {
        readonly get => Start;
        set => Start = value;
    }

    /// <summary>Gets or sets the end point through the generic segment interface.</summary>
    {{Vector3Type}} ISegment3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}},
        {{Box3Type}}, {{Sphere3Type}}>.End
    {
        readonly get => End;
        set => End = value;
    }

    /// <summary>Initializes start and end from the first <see cref="ComponentCount" /> values in a span.</summary>
    public {{TypeName}}(ReadOnlySpan<{{ScalarType}}> values)
        : this(CreateVector(values, 0), CreateVector(values, 3))
    {
    }

    /// <summary>Gets the point halfway between <see cref="Start" /> and <see cref="End" />.</summary>
    public readonly {{Vector3Type}} Center => (Start + End) / {{TwoLiteral}};

    /// <summary>Gets the vector from <see cref="Start" /> to <see cref="End" />.</summary>
    public readonly {{Vector3Type}} Direction => End - Start;

    /// <summary>Gets the distance between <see cref="Start" /> and <see cref="End" />.</summary>
    public readonly {{ScalarType}} Length => Direction.Length;

    /// <summary>Gets the squared distance between <see cref="Start" /> and <see cref="End" />.</summary>
    public readonly {{ScalarType}} LengthSquared => Direction.LengthSquared;

    /// <summary>Creates a segment from start and end points.</summary>
    public static {{TypeName}} Create({{Vector3Type}} start, {{Vector3Type}} end) =>
        new(start, end);

    /// <summary>Creates a segment from the first <see cref="ComponentCount" /> scalar values in a span.</summary>
    public static {{TypeName}} Create(ReadOnlySpan<{{ScalarType}}> values) =>
        new(values);

    /// <summary>Gets or sets a scalar component by zero-based index, with start components first.</summary>
    public {{ScalarType}} this[int index]
    {
        readonly get
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            return index < 3 ? Start[index] : End[index - 3];
        }
        set
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            if (index < 3)
                Start[index] = value;
            else
                End[index - 3] = value;
        }
    }

    /// <summary>Gets a mutable reference to a scalar component by zero-based index.</summary>
    public static ref {{ScalarType}} ComponentRef(ref {{TypeName}} value, int index)
    {
        if ((uint)index >= ComponentCount)
            MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

        if (index < 3)
            return ref {{Vector3Type}}.ComponentRef(ref value.Start, index);

        return ref {{Vector3Type}}.ComponentRef(ref value.End, index - 3);
    }

    /// <summary>Deconstructs the segment into start and end points.</summary>
    public readonly void Deconstruct(out {{Vector3Type}} start, out {{Vector3Type}} end)
    {
        start = Start;
        end = End;
    }

    /// <summary>Copies the start components followed by end components into a caller-owned span.</summary>
    public readonly void CopyTo(Span<{{ScalarType}}> destination)
    {
        if (!TryCopyTo(destination))
            MathsThrowHelper.ThrowDestinationSpanTooShort(ComponentCount);
    }

    /// <summary>Attempts to copy the start components followed by end components into a caller-owned span.</summary>
    public readonly bool TryCopyTo(Span<{{ScalarType}}> destination)
    {
        if (destination.Length < ComponentCount)
            return false;

        Start.CopyTo(destination);
        End.CopyTo(destination[3..]);
        return true;
    }

    /// <summary>Returns the point at <paramref name="amount" /> along the segment without clamping.</summary>
    public readonly {{Vector3Type}} PointAt({{ScalarType}} amount) =>
        Start + (Direction * amount);

    /// <summary>Returns the closest point on this finite segment to <paramref name="point" />.</summary>
    public readonly {{Vector3Type}} ClosestPoint({{Vector3Type}} point)
    {
        var direction = Direction;
        var lengthSquared = direction.LengthSquared;
        if (lengthSquared == {{ZeroLiteral}})
            return Start;

        var amount = {{Vector3Type}}.Dot(point - Start, direction) / lengthSquared;
        return PointAt(ScalarMath.Clamp(amount, {{ZeroLiteral}}, {{OneLiteral}}));
    }

    /// <summary>Returns the squared distance from this finite segment to <paramref name="point" />.</summary>
    public readonly {{ScalarType}} DistanceSquaredTo({{Vector3Type}} point) =>
        {{Vector3Type}}.DistanceSquared(point, ClosestPoint(point));

    /// <summary>Returns the distance from this finite segment to <paramref name="point" />.</summary>
    public readonly {{ScalarType}} DistanceTo({{Vector3Type}} point) =>
        {{Vector3Type}}.Distance(point, ClosestPoint(point));

    /// <summary>Returns whether this segment intersects <paramref name="sphere" />.</summary>
    public readonly bool Intersects({{Sphere3Type}} sphere) =>
        !sphere.IsEmpty && DistanceSquaredTo(sphere.Center) <= sphere.RadiusSquared;

    /// <summary>Returns whether this segment intersects <paramref name="box" />.</summary>
    public readonly bool Intersects({{Box3Type}} box)
    {
        if (box.IsEmpty)
            return false;

        var direction = Direction;
        var near = {{ZeroLiteral}};
        var far = {{OneLiteral}};
        return ClipAxis(Start.X, direction.X, box.Min.X, box.Max.X, ref near, ref far) &&
            ClipAxis(Start.Y, direction.Y, box.Min.Y, box.Max.Y, ref near, ref far) &&
            ClipAxis(Start.Z, direction.Z, box.Min.Z, box.Max.Z, ref near, ref far);
    }

    /// <summary>Attempts to find this segment's intersection amount with <paramref name="plane" />.</summary>
    public readonly bool TryIntersect({{Plane3Type}} plane, out {{ScalarType}} amount)
    {
        var denominator = plane.DotNormal(Direction);
        if (denominator == {{ZeroLiteral}})
        {
            amount = default;
            return false;
        }

        amount = -plane.Evaluate(Start) / denominator;
        if (amount < {{ZeroLiteral}} || amount > {{OneLiteral}})
        {
            amount = default;
            return false;
        }

        return true;
    }

    /// <summary>Clips a segment amount interval against one axis-aligned slab.</summary>
    private readonly bool ClipAxis(
        {{ScalarType}} origin,
        {{ScalarType}} direction,
        {{ScalarType}} min,
        {{ScalarType}} max,
        ref {{ScalarType}} near,
        ref {{ScalarType}} far)
    {
        if (direction == {{ZeroLiteral}})
            return origin >= min && origin <= max;

        var first = (min - origin) / direction;
        var second = (max - origin) / direction;
        if (first > second)
            (first, second) = (second, first);

        near = ScalarMath.Max(near, first);
        far = ScalarMath.Min(far, second);
        return near <= far;
    }

{{CrossScalarConversions}}
    /// <summary>Creates a segment from a start/end point tuple.</summary>
    public static implicit operator {{TypeName}}(({{Vector3Type}} Start, {{Vector3Type}} End) value) =>
        new(value.Start, value.End);

    /// <summary>Returns this segment as a start/end point tuple.</summary>
    public static implicit operator ({{Vector3Type}} Start, {{Vector3Type}} End)({{TypeName}} value) =>
        (value.Start, value.End);

    /// <summary>Returns whether two segments have the same endpoints.</summary>
    public static bool operator ==({{TypeName}} left, {{TypeName}} right) =>
        left.Equals(right);

    /// <summary>Returns whether two segments have different endpoints.</summary>
    public static bool operator !=({{TypeName}} left, {{TypeName}} right) =>
        !left.Equals(right);

    /// <summary>Returns a string representation of this segment.</summary>
    public override readonly string ToString() =>
        ToString(null, null);

    /// <summary>Returns a formatted string representation of this segment.</summary>
    public readonly string ToString(string? format) =>
        ToString(format, null);

    /// <summary>Returns a culture-aware string representation of this segment.</summary>
    public readonly string ToString(IFormatProvider? formatProvider) =>
        ToString(null, formatProvider);

    /// <summary>Returns a formatted, culture-aware string representation of this segment.</summary>
    public readonly string ToString(string? format, IFormatProvider? formatProvider)
    {
        Span<char> destination = stackalloc char[512];
        return TryFormat(destination, out var charsWritten, format.AsSpan(), formatProvider)
            ? new string(destination[..charsWritten])
            : MathsFormatHelper.ToStringSlow(this, format, formatProvider, 1024);
    }

    /// <summary>Attempts to format this segment into a caller-owned character span.</summary>
    public readonly bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        charsWritten = 0;
        var remainder = destination;

        if (!MathsFormatHelper.TryAppend("(".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Start, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(", ".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(End, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(")".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);

        return true;
    }

    /// <summary>Attempts to format this segment into a caller-owned UTF-8 byte span.</summary>
    public readonly bool TryFormat(
        Span<byte> utf8Destination,
        out int bytesWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        bytesWritten = 0;
        var remainder = utf8Destination;

        if (!MathsFormatHelper.TryAppend("("u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Start, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(", "u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(End, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(")"u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);

        return true;
    }

    /// <summary>Compares this segment to another segment by start point, then end point.</summary>
    public readonly int CompareTo({{TypeName}} other)
    {
        var startComparison = Start.CompareTo(other.Start);
        if (startComparison != 0)
            return startComparison;

        return End.CompareTo(other.End);
    }

    /// <summary>Parses a segment from a string.</summary>
    public static {{TypeName}} Parse(string s, IFormatProvider? formatProvider)
    {
        ArgumentNullException.ThrowIfNull(s);
        return Parse(s.AsSpan(), formatProvider);
    }

    /// <summary>Attempts to parse a segment from a string.</summary>
    public static bool TryParse(string? s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (s is null)
        {
            result = default;
            return false;
        }

        return TryParse(s.AsSpan(), formatProvider, out result);
    }

    /// <summary>Parses a segment from a character span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<char> s, IFormatProvider? formatProvider) =>
        TryParse(s, formatProvider, out var result)
            ? result
            : throw new FormatException("Input segment text was not in a correct format.");

    /// <summary>Attempts to parse a segment from a character span.</summary>
    public static bool TryParse(ReadOnlySpan<char> s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(s, out var body))
            return false;

        var separator = MathsTextSeparatorHelper.FindTopLevelSeparator(body);
        if (separator < 0 ||
            !{{Vector3Type}}.TryParse(body[..separator].Trim(), formatProvider, out var start) ||
            !{{Vector3Type}}.TryParse(body[(separator + 1)..].Trim(), formatProvider, out var end))
        {
            return false;
        }

        result = new(start, end);
        return true;
    }

    /// <summary>Parses a segment from a UTF-8 byte span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider) =>
        TryParse(utf8Text, formatProvider, out var result)
            ? result
            : throw new FormatException("Input segment text was not in a correct format.");

    /// <summary>Attempts to parse a segment from a UTF-8 byte span.</summary>
    public static bool TryParse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(utf8Text, out var body))
            return false;

        var separator = MathsTextSeparatorHelper.FindTopLevelSeparator(body);
        if (separator < 0 ||
            !{{Vector3Type}}.TryParse(MathsUtf8TextHelper.TrimAsciiWhitespace(body[..separator]), formatProvider, out var start) ||
            !{{Vector3Type}}.TryParse(MathsUtf8TextHelper.TrimAsciiWhitespace(body[(separator + 1)..]), formatProvider, out var end))
        {
            return false;
        }

        result = new(start, end);
        return true;
    }

    /// <summary>Returns whether this segment has the same endpoints as another segment.</summary>
    public readonly bool Equals({{TypeName}} other) =>
        Start.Equals(other.Start) && End.Equals(other.End);

    /// <summary>Returns whether this segment has the same endpoints as an object.</summary>
    public override readonly bool Equals(object? obj) =>
        obj is {{TypeName}} other && Equals(other);

    /// <summary>Returns a hash code for this segment.</summary>
    public override readonly int GetHashCode() =>
        HashCode.Combine(Start, End);

    /// <summary>Creates a vector from span components.</summary>
    private static {{Vector3Type}} CreateVector(ReadOnlySpan<{{ScalarType}}> values, int offset)
    {
        if (values.Length < ComponentCount)
            MathsThrowHelper.ThrowSourceSpanTooShort(ComponentCount);

        return new(values[offset], values[offset + 1], values[offset + 2]);
    }
}
