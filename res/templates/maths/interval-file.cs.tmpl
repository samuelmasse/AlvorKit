// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>{{TypeSummary}}</summary>
/// <param name="min">The inclusive lower endpoint.</param>
/// <param name="max">The inclusive upper endpoint.</param>
[StructLayout(LayoutKind.Sequential)]
[DebuggerDisplay("{ToString(),nq}")]
[ExcludeFromCodeCoverage]
public struct {{TypeName}}({{ScalarType}} min, {{ScalarType}} max)
    : {{ImplementedInterfaces}}
{
    /// <summary>The number of scalar components in a {{TypeName}}.</summary>
    public const int ComponentCount = 2;

    /// <summary>The byte size of a {{TypeName}}.</summary>
    public const int SizeInBytes = {{SizeBytes}};

    /// <summary>The inclusive lower endpoint.</summary>
    public {{ScalarType}} Min = min;

    /// <summary>The inclusive upper endpoint.</summary>
    public {{ScalarType}} Max = max;

    /// <summary>Gets an empty interval whose minimum is greater than its maximum.</summary>
    public static {{TypeName}} Empty => new({{PositiveInfinity}}, {{NegativeInfinity}});

    /// <summary>Gets the number of scalar components.</summary>
    static int IInterval<{{TypeName}}, {{ScalarType}}>.ComponentCount => ComponentCount;

    /// <summary>Gets the byte size.</summary>
    static int IInterval<{{TypeName}}, {{ScalarType}}>.SizeInBytes => SizeInBytes;

    /// <summary>Gets or sets the lower endpoint through the generic interval interface.</summary>
    {{ScalarType}} IInterval<{{TypeName}}, {{ScalarType}}>.Min
    {
        readonly get => Min;
        set => Min = value;
    }

    /// <summary>Gets or sets the upper endpoint through the generic interval interface.</summary>
    {{ScalarType}} IInterval<{{TypeName}}, {{ScalarType}}>.Max
    {
        readonly get => Max;
        set => Max = value;
    }

    /// <summary>Initializes endpoints from the first <see cref="ComponentCount" /> values in a span.</summary>
    public {{TypeName}}(ReadOnlySpan<{{ScalarType}}> values)
        : this(ComponentFromSpan(values, 0), ComponentFromSpan(values, 1))
    {
    }

    /// <summary>Gets whether the minimum endpoint is greater than the maximum endpoint.</summary>
    public readonly bool IsEmpty => Min > Max;

    /// <summary>Gets the interval length, or zero when the interval is empty.</summary>
    public readonly {{ScalarType}} Length => IsEmpty ? {{ZeroLiteral}} : Max - Min;

    /// <summary>Gets the midpoint between the minimum and maximum endpoints.</summary>
    public readonly {{ScalarType}} Center => (Min + Max) / {{TwoLiteral}};

    /// <summary>Creates an interval from inclusive endpoints, preserving their order.</summary>
    public static {{TypeName}} Create({{ScalarType}} min, {{ScalarType}} max) =>
        new(min, max);

    /// <summary>Creates an interval that contains both endpoints regardless of their order.</summary>
    public static {{TypeName}} CreateFromEndpoints({{ScalarType}} first, {{ScalarType}} second) =>
        new(ScalarMath.Min(first, second), ScalarMath.Max(first, second));

    /// <summary>Creates an interval from the first <see cref="ComponentCount" /> values in a span.</summary>
    public static {{TypeName}} Create(ReadOnlySpan<{{ScalarType}}> values) =>
        new(values);

    /// <summary>Gets or sets an endpoint by zero-based index, with minimum first.</summary>
    public {{ScalarType}} this[int index]
    {
        readonly get
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            return index == 0 ? Min : Max;
        }
        set
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            if (index == 0)
                Min = value;
            else
                Max = value;
        }
    }

    /// <summary>Gets a mutable reference to an endpoint by zero-based index.</summary>
    public static ref {{ScalarType}} ComponentRef(ref {{TypeName}} value, int index)
    {
        if ((uint)index >= ComponentCount)
            MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

        return ref (index == 0 ? ref value.Min : ref value.Max);
    }

    /// <summary>Deconstructs the interval into endpoint components.</summary>
    public readonly void Deconstruct(out {{ScalarType}} min, out {{ScalarType}} max)
    {
        min = Min;
        max = Max;
    }

    /// <summary>Copies the endpoints into a caller-owned span, with minimum first.</summary>
    public readonly void CopyTo(Span<{{ScalarType}}> destination)
    {
        if (!TryCopyTo(destination))
            MathsThrowHelper.ThrowDestinationSpanTooShort(ComponentCount);
    }

    /// <summary>Attempts to copy the endpoints into a caller-owned span, with minimum first.</summary>
    public readonly bool TryCopyTo(Span<{{ScalarType}}> destination)
    {
        if (destination.Length < ComponentCount)
            return false;

        destination[0] = Min;
        destination[1] = Max;
        return true;
    }

    /// <summary>Returns whether this interval contains <paramref name="value" />.</summary>
    public readonly bool Contains({{ScalarType}} value) =>
        !IsEmpty && value >= Min && value <= Max;

    /// <summary>Returns whether this interval fully contains <paramref name="other" />.</summary>
    public readonly bool Contains({{TypeName}} other) =>
        !IsEmpty && !other.IsEmpty && other.Min >= Min && other.Max <= Max;

    /// <summary>Returns whether this interval overlaps <paramref name="other" />.</summary>
    public readonly bool Intersects({{TypeName}} other) =>
        !IsEmpty && !other.IsEmpty && Min <= other.Max && other.Min <= Max;

    /// <summary>Returns the smallest interval containing both intervals.</summary>
    public static {{TypeName}} Union({{TypeName}} left, {{TypeName}} right)
    {
        if (left.IsEmpty)
            return right;
        if (right.IsEmpty)
            return left;

        return new(ScalarMath.Min(left.Min, right.Min), ScalarMath.Max(left.Max, right.Max));
    }

    /// <summary>Returns the overlapping interval shared by both intervals.</summary>
    public static {{TypeName}} Intersection({{TypeName}} left, {{TypeName}} right)
    {
        if (left.IsEmpty || right.IsEmpty)
            return Empty;

        var min = ScalarMath.Max(left.Min, right.Min);
        var max = ScalarMath.Min(left.Max, right.Max);
        return min <= max ? new {{TypeName}}(min, max) : Empty;
    }

{{CrossScalarConversions}}
    /// <summary>Creates an interval from a minimum/maximum endpoint tuple.</summary>
    public static implicit operator {{TypeName}}(({{ScalarType}} Min, {{ScalarType}} Max) value) =>
        new(value.Min, value.Max);

    /// <summary>Returns this interval as a minimum/maximum endpoint tuple.</summary>
    public static implicit operator ({{ScalarType}} Min, {{ScalarType}} Max)({{TypeName}} value) =>
        (value.Min, value.Max);

    /// <summary>Returns whether two intervals have the same endpoints.</summary>
    public static bool operator ==({{TypeName}} left, {{TypeName}} right) =>
        left.Equals(right);

    /// <summary>Returns whether two intervals have different endpoints.</summary>
    public static bool operator !=({{TypeName}} left, {{TypeName}} right) =>
        !left.Equals(right);

    /// <summary>Returns a string representation of this interval.</summary>
    public override readonly string ToString() =>
        ToString(null, null);

    /// <summary>Returns a formatted string representation of this interval.</summary>
    public readonly string ToString(string? format) =>
        ToString(format, null);

    /// <summary>Returns a culture-aware string representation of this interval.</summary>
    public readonly string ToString(IFormatProvider? formatProvider) =>
        ToString(null, formatProvider);

    /// <summary>Returns a formatted, culture-aware string representation of this interval.</summary>
    public readonly string ToString(string? format, IFormatProvider? formatProvider)
    {
        Span<char> destination = stackalloc char[128];
        return TryFormat(destination, out var charsWritten, format.AsSpan(), formatProvider)
            ? new string(destination[..charsWritten])
            : MathsFormatHelper.ToStringSlow(this, format, formatProvider, 256);
    }

    /// <summary>Attempts to format this interval into a caller-owned character span.</summary>
    public readonly bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        charsWritten = 0;
        var remainder = destination;

        if (!MathsFormatHelper.TryAppend("(".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Min, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(", ".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Max, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(")".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);

        return true;
    }

    /// <summary>Attempts to format this interval into a caller-owned UTF-8 byte span.</summary>
    public readonly bool TryFormat(
        Span<byte> utf8Destination,
        out int bytesWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        bytesWritten = 0;
        var remainder = utf8Destination;

        if (!MathsFormatHelper.TryAppend("("u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Min, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(", "u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(Max, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(")"u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);

        return true;
    }

    /// <summary>Compares this interval to another interval by minimum endpoint, then maximum endpoint.</summary>
    public readonly int CompareTo({{TypeName}} other)
    {
        var minComparison = Min.CompareTo(other.Min);
        if (minComparison != 0)
            return minComparison;

        return Max.CompareTo(other.Max);
    }

    /// <summary>Parses an interval from a string.</summary>
    public static {{TypeName}} Parse(string s, IFormatProvider? formatProvider)
    {
        ArgumentNullException.ThrowIfNull(s);
        return Parse(s.AsSpan(), formatProvider);
    }

    /// <summary>Attempts to parse an interval from a string.</summary>
    public static bool TryParse(string? s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (s is null)
        {
            result = default;
            return false;
        }

        return TryParse(s.AsSpan(), formatProvider, out result);
    }

    /// <summary>Parses an interval from a character span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<char> s, IFormatProvider? formatProvider) =>
        TryParse(s, formatProvider, out var result)
            ? result
            : throw new FormatException("Input interval text was not in a correct format.");

    /// <summary>Attempts to parse an interval from a character span.</summary>
    public static bool TryParse(ReadOnlySpan<char> s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(s, out var body))
            return false;

        var separator = MathsTextSeparatorHelper.FindFlatSeparator(body);
        if (separator < 0 ||
            !MathsParseHelper.TryParseComponent(body[..separator].Trim(), formatProvider, out {{ScalarType}} min) ||
            !MathsParseHelper.TryParseComponent(body[(separator + 1)..].Trim(), formatProvider, out {{ScalarType}} max))
        {
            return false;
        }

        result = new(min, max);
        return true;
    }

    /// <summary>Parses an interval from a UTF-8 byte span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider) =>
        TryParse(utf8Text, formatProvider, out var result)
            ? result
            : throw new FormatException("Input interval text was not in a correct format.");

    /// <summary>Attempts to parse an interval from a UTF-8 byte span.</summary>
    public static bool TryParse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(utf8Text, out var body))
            return false;

        var separator = MathsTextSeparatorHelper.FindFlatSeparator(body);
        if (separator < 0 ||
            !MathsParseHelper.TryParseComponent(
                MathsUtf8TextHelper.TrimAsciiWhitespace(body[..separator]),
                formatProvider,
                out {{ScalarType}} min) ||
            !MathsParseHelper.TryParseComponent(
                MathsUtf8TextHelper.TrimAsciiWhitespace(body[(separator + 1)..]),
                formatProvider,
                out {{ScalarType}} max))
        {
            return false;
        }

        result = new(min, max);
        return true;
    }

    /// <summary>Returns whether this interval has the same endpoints as another interval.</summary>
    public readonly bool Equals({{TypeName}} other) =>
        Min.Equals(other.Min) && Max.Equals(other.Max);

    /// <summary>Returns whether this interval has the same endpoints as an object.</summary>
    public override readonly bool Equals(object? obj) =>
        obj is {{TypeName}} other && Equals(other);

    /// <summary>Returns a hash code for this interval.</summary>
    public override readonly int GetHashCode() =>
        HashCode.Combine(Min, Max);

    /// <summary>Reads one constructor component after validating span length.</summary>
    private static {{ScalarType}} ComponentFromSpan(ReadOnlySpan<{{ScalarType}}> values, int index)
    {
        if (values.Length < ComponentCount)
            MathsThrowHelper.ThrowSourceSpanTooShort(ComponentCount);

        return values[index];
    }
}
