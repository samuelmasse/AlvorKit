// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>{{TypeSummary}}</summary>
/// <param name="x">The X component of the vector part.</param>
/// <param name="y">The Y component of the vector part.</param>
/// <param name="z">The Z component of the vector part.</param>
/// <param name="w">The scalar component.</param>
[StructLayout(LayoutKind.Sequential)]
[DebuggerDisplay("{ToString(),nq}")]
[ExcludeFromCodeCoverage]
public struct {{TypeName}}({{ScalarType}} x, {{ScalarType}} y, {{ScalarType}} z, {{ScalarType}} w)
    : {{ImplementedInterfaces}}
{
    /// <summary>The number of scalar components in a {{TypeName}}.</summary>
    public const int ComponentCount = 4;

    /// <summary>The byte size of a {{TypeName}}.</summary>
    public const int SizeInBytes = {{SizeBytes}};

    /// <summary>The X component of the vector part.</summary>
    public {{ScalarType}} X = x;

    /// <summary>The Y component of the vector part.</summary>
    public {{ScalarType}} Y = y;

    /// <summary>The Z component of the vector part.</summary>
    public {{ScalarType}} Z = z;

    /// <summary>The scalar component.</summary>
    public {{ScalarType}} W = w;

    /// <summary>Creates a quaternion from vector and scalar parts.</summary>
    public {{TypeName}}({{Vector3Type}} vector, {{ScalarType}} scalar)
        : this(vector.X, vector.Y, vector.Z, scalar)
    {
    }

    /// <summary>Initializes components from the first <see cref="ComponentCount" /> values in a span.</summary>
    public {{TypeName}}(ReadOnlySpan<{{ScalarType}}> values)
        : this(
            ComponentFromSpan(values, 0),
            ComponentFromSpan(values, 1),
            ComponentFromSpan(values, 2),
            ComponentFromSpan(values, 3))
    {
    }

    /// <summary>Gets the zero quaternion.</summary>
    public static {{TypeName}} Zero => default;

    /// <summary>Gets the identity rotation quaternion.</summary>
    public static {{TypeName}} Identity => new({{ZeroLiteral}}, {{ZeroLiteral}}, {{ZeroLiteral}}, {{OneLiteral}});

    /// <summary>Gets the number of scalar components.</summary>
    static int IQuat<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{MaskType}}, {{Matrix3Type}}, {{Matrix4Type}}>.ComponentCount =>
        ComponentCount;

    /// <summary>Gets the byte size.</summary>
    static int IQuat<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{MaskType}}, {{Matrix3Type}}, {{Matrix4Type}}>.SizeInBytes =>
        SizeInBytes;

    /// <summary>Gets the additive identity.</summary>
    static {{TypeName}} IAdditiveIdentity<{{TypeName}}, {{TypeName}}>.AdditiveIdentity => Zero;

    /// <summary>Gets the multiplicative identity.</summary>
    static {{TypeName}} IMultiplicativeIdentity<{{TypeName}}, {{TypeName}}>.MultiplicativeIdentity => Identity;

    /// <summary>Gets or sets the vector part.</summary>
    public {{Vector3Type}} Vector
    {
        readonly get => new(X, Y, Z);
        set
        {
            X = value.X;
            Y = value.Y;
            Z = value.Z;
        }
    }

    /// <summary>Gets or sets the scalar part.</summary>
    public {{ScalarType}} Scalar
    {
        readonly get => W;
        set => W = value;
    }

    /// <summary>Gets a component vector for delegated component helpers.</summary>
    private readonly {{Vector4Type}} Components => new(X, Y, Z, W);

    /// <summary>Creates a quaternion from scalar components.</summary>
    public static {{TypeName}} Create({{ScalarType}} x, {{ScalarType}} y, {{ScalarType}} z, {{ScalarType}} w) =>
        new(x, y, z, w);

    /// <summary>Creates a quaternion from vector and scalar parts.</summary>
    public static {{TypeName}} Create({{Vector3Type}} vector, {{ScalarType}} scalar) =>
        new(vector, scalar);

    /// <summary>Creates a quaternion from the first <see cref="ComponentCount" /> values in a span.</summary>
    public static {{TypeName}} Create(ReadOnlySpan<{{ScalarType}}> values) =>
        new(values);

    /// <summary>Gets or sets a component by zero-based index.</summary>
    public {{ScalarType}} this[int index]
    {
        readonly get
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            return Unsafe.Add(ref Unsafe.AsRef(in X), index);
        }
        set
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            Unsafe.Add(ref X, index) = value;
        }
    }

    /// <summary>Gets a mutable reference to a component by zero-based index.</summary>
    public static ref {{ScalarType}} ComponentRef(ref {{TypeName}} value, int index)
    {
        if ((uint)index >= ComponentCount)
            MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

        return ref Unsafe.Add(ref value.X, index);
    }

    /// <summary>Deconstructs the quaternion into scalar components.</summary>
    public readonly void Deconstruct(
        out {{ScalarType}} x,
        out {{ScalarType}} y,
        out {{ScalarType}} z,
        out {{ScalarType}} w)
    {
        x = X;
        y = Y;
        z = Z;
        w = W;
    }

    /// <summary>Copies this quaternion into an array.</summary>
    public readonly void CopyTo({{ScalarType}}[] array)
    {
        ArgumentNullException.ThrowIfNull(array);
        CopyTo(array.AsSpan());
    }

    /// <summary>Copies this quaternion into an array starting at <paramref name="index" />.</summary>
    public readonly void CopyTo({{ScalarType}}[] array, int index)
    {
        ArgumentNullException.ThrowIfNull(array);
        CopyTo(array.AsSpan(index));
    }

    /// <summary>Copies this quaternion into a destination span.</summary>
    public readonly void CopyTo(Span<{{ScalarType}}> destination)
    {
        if (!TryCopyTo(destination))
            MathsThrowHelper.ThrowDestinationSpanTooShort(ComponentCount);
    }

    /// <summary>Attempts to copy this quaternion into a destination span.</summary>
    public readonly bool TryCopyTo(Span<{{ScalarType}}> destination)
    {
        if (destination.Length < ComponentCount)
            return false;

        destination[0] = X;
        destination[1] = Y;
        destination[2] = Z;
        destination[3] = W;
        return true;
    }

    /// <summary>Reads one constructor component after validating span length.</summary>
    private static {{ScalarType}} ComponentFromSpan(ReadOnlySpan<{{ScalarType}}> values, int index)
    {
        if (values.Length < ComponentCount)
            MathsThrowHelper.ThrowSourceSpanTooShort(ComponentCount);

        return values[index];
    }

    /// <summary>Returns the squared quaternion length.</summary>
    public readonly {{ScalarType}} LengthSquared => Dot(this, this);

    /// <summary>Returns the quaternion length.</summary>
    public readonly {{ScalarType}} Length => ScalarMath.Sqrt(LengthSquared);

    /// <summary>Gets this quaternion divided by its length.</summary>
    public readonly {{TypeName}} Normalized => this / Length;

    /// <summary>Gets this quaternion divided by its length, or identity when its length is zero.</summary>
    public readonly {{TypeName}} NormalizedOrIdentity => NormalizedOr(Identity);

    /// <summary>Returns this quaternion divided by its length, or fallback when its length is zero.</summary>
    public readonly {{TypeName}} NormalizedOr({{TypeName}} fallback) =>
        LengthSquared > {{ZeroLiteral}} ? this / Length : fallback;

    /// <summary>Returns this quaternion divided by its length when possible.</summary>
    public readonly bool TryNormalize(out {{TypeName}} result)
    {
        var lengthSquared = LengthSquared;
        if (lengthSquared <= {{ZeroLiteral}})
        {
            result = default;
            return false;
        }

        result = this / ScalarMath.Sqrt(lengthSquared);
        return true;
    }

    /// <summary>Returns value divided by its length.</summary>
    public static {{TypeName}} Normalize({{TypeName}} value) =>
        value.Normalized;

    /// <summary>Returns the dot product of two quaternions.</summary>
    public static {{ScalarType}} Dot({{TypeName}} left, {{TypeName}} right) =>
        (left.X * right.X) + (left.Y * right.Y) + (left.Z * right.Z) + (left.W * right.W);

    /// <summary>Gets the conjugate quaternion.</summary>
    public readonly {{TypeName}} Conjugated => Conjugate(this);

    /// <summary>Gets the inverse quaternion.</summary>
    public readonly {{TypeName}} Inverted => Invert(this);

    /// <summary>Returns the conjugate quaternion.</summary>
    public static {{TypeName}} Conjugate({{TypeName}} value) =>
        new(-value.X, -value.Y, -value.Z, value.W);

    /// <summary>Returns the inverse quaternion.</summary>
    public static {{TypeName}} Invert({{TypeName}} value) =>
        Conjugate(value) / value.LengthSquared;

    /// <summary>Returns the inverse quaternion when possible.</summary>
    public static bool TryInvert({{TypeName}} value, out {{TypeName}} result)
    {
        var lengthSquared = value.LengthSquared;
        if (lengthSquared <= {{ZeroLiteral}})
        {
            result = default;
            return false;
        }

        result = Conjugate(value) / lengthSquared;
        return true;
    }

    /// <summary>Returns whether value is close to the identity rotation.</summary>
    public static bool IsIdentity({{TypeName}} value, {{ScalarType}} epsilon) =>
        Equal(value, Identity, epsilon).All;

    /// <summary>Returns whether this value is close to the identity rotation.</summary>
    public readonly bool IsIdentity({{ScalarType}} epsilon) =>
        IsIdentity(this, epsilon);

    /// <summary>Returns whether value has unit length.</summary>
    public static bool IsNormalized({{TypeName}} value, {{ScalarType}} epsilon) =>
        ScalarMath.Abs(value.LengthSquared - {{OneLiteral}}) <= epsilon;

    /// <summary>Returns whether this value has unit length.</summary>
    public readonly bool IsNormalized({{ScalarType}} epsilon) =>
        IsNormalized(this, epsilon);

    /// <summary>Adds two quaternions component by component.</summary>
    public static {{TypeName}} operator +({{TypeName}} left, {{TypeName}} right) =>
        new(left.X + right.X, left.Y + right.Y, left.Z + right.Z, left.W + right.W);

    /// <summary>Adds a scalar to each quaternion component.</summary>
    public static {{TypeName}} operator +({{TypeName}} left, {{ScalarType}} right) =>
        new(left.X + right, left.Y + right, left.Z + right, left.W + right);

    /// <summary>Adds a scalar to each quaternion component.</summary>
    public static {{TypeName}} operator +({{ScalarType}} left, {{TypeName}} right) =>
        new(left + right.X, left + right.Y, left + right.Z, left + right.W);

    /// <summary>Returns this quaternion unchanged.</summary>
    public static {{TypeName}} operator +({{TypeName}} value) =>
        value;

    /// <summary>Subtracts two quaternions component by component.</summary>
    public static {{TypeName}} operator -({{TypeName}} left, {{TypeName}} right) =>
        new(left.X - right.X, left.Y - right.Y, left.Z - right.Z, left.W - right.W);

    /// <summary>Subtracts a scalar from each quaternion component.</summary>
    public static {{TypeName}} operator -({{TypeName}} left, {{ScalarType}} right) =>
        new(left.X - right, left.Y - right, left.Z - right, left.W - right);

    /// <summary>Subtracts each quaternion component from a scalar.</summary>
    public static {{TypeName}} operator -({{ScalarType}} left, {{TypeName}} right) =>
        new(left - right.X, left - right.Y, left - right.Z, left - right.W);

    /// <summary>Negates each quaternion component.</summary>
    public static {{TypeName}} operator -({{TypeName}} value) =>
        new(-value.X, -value.Y, -value.Z, -value.W);

    /// <summary>Concatenates two rotations using the Hamilton product.</summary>
    public static {{TypeName}} operator *({{TypeName}} left, {{TypeName}} right) =>
        new(
            (left.W * right.X) + (left.X * right.W) + (left.Y * right.Z) - (left.Z * right.Y),
            (left.W * right.Y) + (left.Y * right.W) + (left.Z * right.X) - (left.X * right.Z),
            (left.W * right.Z) + (left.Z * right.W) + (left.X * right.Y) - (left.Y * right.X),
            (left.W * right.W) - (left.X * right.X) - (left.Y * right.Y) - (left.Z * right.Z));

    /// <summary>Multiplies each quaternion component by a scalar.</summary>
    public static {{TypeName}} operator *({{TypeName}} left, {{ScalarType}} right) =>
        new(left.X * right, left.Y * right, left.Z * right, left.W * right);

    /// <summary>Multiplies each quaternion component by a scalar.</summary>
    public static {{TypeName}} operator *({{ScalarType}} left, {{TypeName}} right) =>
        right * left;

    /// <summary>Rotates a vector by a normalized quaternion.</summary>
    public static {{Vector3Type}} operator *({{TypeName}} rotation, {{Vector3Type}} vector) =>
        TransformVector(rotation, vector);

    /// <summary>Divides one quaternion by another using the inverse of the right operand.</summary>
    public static {{TypeName}} operator /({{TypeName}} left, {{TypeName}} right) =>
        left * Invert(right);

    /// <summary>Divides each quaternion component by a scalar.</summary>
    public static {{TypeName}} operator /({{TypeName}} left, {{ScalarType}} right) =>
        new(left.X / right, left.Y / right, left.Z / right, left.W / right);

    /// <summary>Divides a scalar by each quaternion component.</summary>
    public static {{TypeName}} operator /({{ScalarType}} left, {{TypeName}} right) =>
        new(left / right.X, left / right.Y, left / right.Z, left / right.W);

    /// <summary>Adds two quaternions component by component.</summary>
    public static {{TypeName}} Add({{TypeName}} left, {{TypeName}} right) =>
        left + right;

    /// <summary>Subtracts two quaternions component by component.</summary>
    public static {{TypeName}} Subtract({{TypeName}} left, {{TypeName}} right) =>
        left - right;

    /// <summary>Concatenates two rotations using the Hamilton product.</summary>
    public static {{TypeName}} Multiply({{TypeName}} left, {{TypeName}} right) =>
        left * right;

    /// <summary>Divides one quaternion by another using the inverse of the right operand.</summary>
    public static {{TypeName}} Divide({{TypeName}} left, {{TypeName}} right) =>
        left / right;

    /// <summary>Negates each quaternion component.</summary>
    public static {{TypeName}} Negate({{TypeName}} value) =>
        -value;

    /// <summary>Creates a quaternion from an axis and angle in radians.</summary>
    public static {{TypeName}} CreateFromAxisAngle({{Vector3Type}} axis, {{ScalarType}} radians)
    {
        var halfAngle = radians * {{HalfLiteral}};
        var scale = ScalarMath.Sin(halfAngle);
        return new(axis * scale, ScalarMath.Cos(halfAngle));
    }

    /// <summary>Creates a quaternion from pitch, yaw, and roll angles in radians.</summary>
    public static {{TypeName}} CreateFromEulerAngles({{Vector3Type}} angles) =>
        CreateFromYawPitchRoll(angles.Y, angles.X, angles.Z);

    /// <summary>Creates a quaternion from yaw, pitch, and roll angles in radians.</summary>
    public static {{TypeName}} CreateFromYawPitchRoll({{ScalarType}} yaw, {{ScalarType}} pitch, {{ScalarType}} roll)
    {
        var halfYaw = yaw * {{HalfLiteral}};
        var sy = ScalarMath.Sin(halfYaw);
        var cy = ScalarMath.Cos(halfYaw);
        var halfPitch = pitch * {{HalfLiteral}};
        var sp = ScalarMath.Sin(halfPitch);
        var cp = ScalarMath.Cos(halfPitch);
        var halfRoll = roll * {{HalfLiteral}};
        var sr = ScalarMath.Sin(halfRoll);
        var cr = ScalarMath.Cos(halfRoll);

        return new(
            (cy * sp * cr) + (sy * cp * sr),
            (sy * cp * cr) - (cy * sp * sr),
            (cy * cp * sr) - (sy * sp * cr),
            (cy * cp * cr) + (sy * sp * sr));
    }

    /// <summary>Creates a quaternion from a 3x3 rotation matrix.</summary>
    public static {{TypeName}} CreateFromRotationMatrix({{Matrix3Type}} matrix)
    {
        var fourXSquaredMinusOne = matrix[0, 0] - matrix[1, 1] - matrix[2, 2];
        var fourYSquaredMinusOne = matrix[1, 1] - matrix[0, 0] - matrix[2, 2];
        var fourZSquaredMinusOne = matrix[2, 2] - matrix[0, 0] - matrix[1, 1];
        var fourWSquaredMinusOne = matrix[0, 0] + matrix[1, 1] + matrix[2, 2];
        var biggestIndex = 0;
        var fourBiggestSquaredMinusOne = fourWSquaredMinusOne;

        if (fourXSquaredMinusOne > fourBiggestSquaredMinusOne)
        {
            fourBiggestSquaredMinusOne = fourXSquaredMinusOne;
            biggestIndex = 1;
        }

        if (fourYSquaredMinusOne > fourBiggestSquaredMinusOne)
        {
            fourBiggestSquaredMinusOne = fourYSquaredMinusOne;
            biggestIndex = 2;
        }

        if (fourZSquaredMinusOne > fourBiggestSquaredMinusOne)
        {
            fourBiggestSquaredMinusOne = fourZSquaredMinusOne;
            biggestIndex = 3;
        }

        var biggestValue = ScalarMath.Sqrt(fourBiggestSquaredMinusOne + {{OneLiteral}}) * {{HalfLiteral}};
        var multiplier = {{QuarterLiteral}} / biggestValue;
        return biggestIndex switch
        {
            0 => new(
                (matrix[1, 2] - matrix[2, 1]) * multiplier,
                (matrix[2, 0] - matrix[0, 2]) * multiplier,
                (matrix[0, 1] - matrix[1, 0]) * multiplier,
                biggestValue),
            1 => new(
                biggestValue,
                (matrix[0, 1] + matrix[1, 0]) * multiplier,
                (matrix[2, 0] + matrix[0, 2]) * multiplier,
                (matrix[1, 2] - matrix[2, 1]) * multiplier),
            2 => new(
                (matrix[0, 1] + matrix[1, 0]) * multiplier,
                biggestValue,
                (matrix[1, 2] + matrix[2, 1]) * multiplier,
                (matrix[2, 0] - matrix[0, 2]) * multiplier),
            _ => new(
                (matrix[2, 0] + matrix[0, 2]) * multiplier,
                (matrix[1, 2] + matrix[2, 1]) * multiplier,
                biggestValue,
                (matrix[0, 1] - matrix[1, 0]) * multiplier),
        };
    }

    /// <summary>Creates a quaternion from a 4x4 rotation matrix.</summary>
    public static {{TypeName}} CreateFromRotationMatrix({{Matrix4Type}} matrix) =>
        CreateFromRotationMatrix(new {{Matrix3Type}}(
            new {{Vector3Type}}(matrix.Column0.X, matrix.Column0.Y, matrix.Column0.Z),
            new {{Vector3Type}}(matrix.Column1.X, matrix.Column1.Y, matrix.Column1.Z),
            new {{Vector3Type}}(matrix.Column2.X, matrix.Column2.Y, matrix.Column2.Z)));

    /// <summary>Creates a right-handed orientation that points local negative Z along direction.</summary>
    private static {{TypeName}} CreateLookRotationRightHanded({{Vector3Type}} direction, {{Vector3Type}} up)
    {
        var forward = {{Vector3Type}}.Normalize(direction);
        var side = {{Vector3Type}}.Normalize({{Vector3Type}}.Cross(forward, up));
        var actualUp = {{Vector3Type}}.Cross(side, forward);
        return CreateFromRotationMatrix(new {{Matrix3Type}}(side, actualUp, -forward));
    }

    /// <summary>Creates a left-handed orientation that points local positive Z along direction.</summary>
    private static {{TypeName}} CreateLookRotationLeftHanded({{Vector3Type}} direction, {{Vector3Type}} up)
    {
        var forward = {{Vector3Type}}.Normalize(direction);
        var side = {{Vector3Type}}.Normalize({{Vector3Type}}.Cross(up, forward));
        var actualUp = {{Vector3Type}}.Cross(forward, side);
        return CreateFromRotationMatrix(new {{Matrix3Type}}(side, actualUp, forward));
    }

    /// <summary>Creates an orientation with default OpenGL view-space conventions.</summary>
    public static {{TypeName}} LookRotation({{Vector3Type}} direction, {{Vector3Type}} up) =>
        LookRotation(direction, up, ProjectionHandedness.Right);

    /// <summary>Creates an orientation with explicit view-space handedness.</summary>
    public static {{TypeName}} LookRotation({{Vector3Type}} direction, {{Vector3Type}} up, ProjectionHandedness handedness) =>
        handedness switch
        {
            ProjectionHandedness.Right => CreateLookRotationRightHanded(direction, up),
            ProjectionHandedness.Left => CreateLookRotationLeftHanded(direction, up),
            _ => throw new ArgumentOutOfRangeException(nameof(handedness), "Unsupported view-space handedness."),
        };

    /// <summary>Creates the shortest rotation from one direction to another.</summary>
    public static {{TypeName}} CreateRotationBetween({{Vector3Type}} from, {{Vector3Type}} to)
    {
        var normalizedFrom = {{Vector3Type}}.Normalize(from);
        var normalizedTo = {{Vector3Type}}.Normalize(to);
        var cosTheta = {{Vector3Type}}.Dot(normalizedFrom, normalizedTo);
        if (cosTheta >= {{OneLiteral}} - {{ToleranceLiteral}})
            return Identity;

        if (cosTheta < {{MinusOneLiteral}} + {{ToleranceLiteral}})
        {
            var rotationAxis = {{Vector3Type}}.Cross({{Vector3Type}}.UnitZ, normalizedFrom);
            if (rotationAxis.LengthSquared < {{ToleranceLiteral}})
                rotationAxis = {{Vector3Type}}.Cross({{Vector3Type}}.UnitX, normalizedFrom);

            return CreateFromAxisAngle(rotationAxis.Normalized, {{PiLiteral}});
        }

        var axis = {{Vector3Type}}.Cross(normalizedFrom, normalizedTo);
        var scale = ScalarMath.Sqrt(({{OneLiteral}} + cosTheta) * {{TwoLiteral}});
        var inverseScale = {{OneLiteral}} / scale;
        return new {{TypeName}}(axis * inverseScale, scale * {{HalfLiteral}}).Normalized;
    }

    /// <summary>Gets the rotation angle in radians.</summary>
    public readonly {{ScalarType}} Angle
    {
        get
        {
            var normalized = NormalizedOrIdentity;
            var vectorLength = normalized.Vector.Length;
            return vectorLength > {{ToleranceLiteral}}
                ? {{TwoLiteral}} * ScalarMath.Atan2(vectorLength, normalized.W)
                : {{ZeroLiteral}};
        }
    }

    /// <summary>Gets the rotation axis.</summary>
    public readonly {{Vector3Type}} Axis
    {
        get
        {
            var normalized = NormalizedOrIdentity;
            var vectorLength = normalized.Vector.Length;
            return vectorLength > {{ToleranceLiteral}} ? normalized.Vector / vectorLength : {{Vector3Type}}.UnitZ;
        }
    }

    /// <summary>Gets pitch, yaw, and roll in radians.</summary>
    public readonly {{Vector3Type}} EulerAngles => new(Pitch, Yaw, Roll);

    /// <summary>Gets the pitch angle in radians.</summary>
    public readonly {{ScalarType}} Pitch
    {
        get
        {
            var y = {{TwoLiteral}} * ((Y * Z) + (W * X));
            var x = (W * W) - (X * X) - (Y * Y) + (Z * Z);
            return ScalarMath.Abs(x) <= {{ToleranceLiteral}} && ScalarMath.Abs(y) <= {{ToleranceLiteral}}
                ? {{TwoLiteral}} * ScalarMath.Atan2(X, W)
                : ScalarMath.Atan2(y, x);
        }
    }

    /// <summary>Gets the yaw angle in radians.</summary>
    public readonly {{ScalarType}} Yaw =>
        ScalarMath.Asin(ScalarMath.Clamp(-{{TwoLiteral}} * ((X * Z) - (W * Y)), {{MinusOneLiteral}}, {{OneLiteral}}));

    /// <summary>Gets the roll angle in radians.</summary>
    public readonly {{ScalarType}} Roll
    {
        get
        {
            var y = {{TwoLiteral}} * ((X * Y) + (W * Z));
            var x = (W * W) + (X * X) - (Y * Y) - (Z * Z);
            return ScalarMath.Abs(x) <= {{ToleranceLiteral}} && ScalarMath.Abs(y) <= {{ToleranceLiteral}}
                ? {{ZeroLiteral}}
                : ScalarMath.Atan2(y, x);
        }
    }

    /// <summary>Writes the quaternion as an axis and angle in radians.</summary>
    public readonly void ToAxisAngle(out {{Vector3Type}} axis, out {{ScalarType}} radians)
    {
        axis = Axis;
        radians = Angle;
    }

    /// <summary>Converts the quaternion to a 3x3 rotation matrix.</summary>
    public readonly {{Matrix3Type}} ToMat3()
    {
        var qxx = X * X;
        var qyy = Y * Y;
        var qzz = Z * Z;
        var qxz = X * Z;
        var qxy = X * Y;
        var qyz = Y * Z;
        var qwx = W * X;
        var qwy = W * Y;
        var qwz = W * Z;

        return new(
            new {{Vector3Type}}(
                {{OneLiteral}} - ({{TwoLiteral}} * (qyy + qzz)),
                {{TwoLiteral}} * (qxy + qwz),
                {{TwoLiteral}} * (qxz - qwy)),
            new {{Vector3Type}}(
                {{TwoLiteral}} * (qxy - qwz),
                {{OneLiteral}} - ({{TwoLiteral}} * (qxx + qzz)),
                {{TwoLiteral}} * (qyz + qwx)),
            new {{Vector3Type}}(
                {{TwoLiteral}} * (qxz + qwy),
                {{TwoLiteral}} * (qyz - qwx),
                {{OneLiteral}} - ({{TwoLiteral}} * (qxx + qyy))));
    }

    /// <summary>Converts the quaternion to a 4x4 rotation matrix.</summary>
    public readonly {{Matrix4Type}} ToMat4()
    {
        var matrix = ToMat3();
        return new(
            new {{Vector4Type}}(matrix.Column0, {{ZeroLiteral}}),
            new {{Vector4Type}}(matrix.Column1, {{ZeroLiteral}}),
            new {{Vector4Type}}(matrix.Column2, {{ZeroLiteral}}),
            new {{Vector4Type}}({{ZeroLiteral}}, {{ZeroLiteral}}, {{ZeroLiteral}}, {{OneLiteral}}));
    }

    /// <summary>Rotates a vector by a normalized quaternion.</summary>
    public static {{Vector3Type}} TransformVector({{TypeName}} rotation, {{Vector3Type}} vector)
    {
        var uv = {{Vector3Type}}.Cross(rotation.Vector, vector);
        var uuv = {{Vector3Type}}.Cross(rotation.Vector, uv);
        return vector + (((uv * rotation.W) + uuv) * {{TwoLiteral}});
    }

    /// <summary>Linearly interpolates between two quaternions without normalizing.</summary>
    public static {{TypeName}} Lerp({{TypeName}} from, {{TypeName}} to, {{ScalarType}} amount) =>
        (from * ({{OneLiteral}} - amount)) + (to * amount);

    /// <summary>Linearly interpolates between two quaternions and normalizes the result.</summary>
    public static {{TypeName}} Nlerp({{TypeName}} from, {{TypeName}} to, {{ScalarType}} amount) =>
        Normalize(Lerp(from, Dot(from, to) < {{ZeroLiteral}} ? -to : to, amount));

    /// <summary>Spherically interpolates along the shortest arc.</summary>
    public static {{TypeName}} Slerp({{TypeName}} from, {{TypeName}} to, {{ScalarType}} amount)
    {
        var end = to;
        var cosTheta = Dot(from, to);
        if (cosTheta < {{ZeroLiteral}})
        {
            end = -to;
            cosTheta = -cosTheta;
        }

        if (cosTheta > {{OneLiteral}} - {{ToleranceLiteral}})
            return Nlerp(from, end, amount);

        var angle = ScalarMath.Acos(cosTheta);
        return ((ScalarMath.Sin(({{OneLiteral}} - amount) * angle) * from) + (ScalarMath.Sin(amount * angle) * end)) /
            ScalarMath.Sin(angle);
    }

    /// <summary>Spherically interpolates with extra spins along the arc.</summary>
    public static {{TypeName}} Slerp({{TypeName}} from, {{TypeName}} to, {{ScalarType}} amount, int extraSpins)
    {
        var end = to;
        var cosTheta = Dot(from, to);
        if (cosTheta < {{ZeroLiteral}})
        {
            end = -to;
            cosTheta = -cosTheta;
        }

        if (cosTheta > {{OneLiteral}} - {{ToleranceLiteral}})
            return Nlerp(from, end, amount);

        var angle = ScalarMath.Acos(cosTheta);
        var phi = angle + (extraSpins * {{PiLiteral}});
        return ((ScalarMath.Sin(angle - (amount * phi)) * from) + (ScalarMath.Sin(amount * phi) * end)) / ScalarMath.Sin(angle);
    }

    /// <summary>Interpolates between quaternions using squad control points.</summary>
    public static {{TypeName}} Squad(
        {{TypeName}} from,
        {{TypeName}} to,
        {{TypeName}} fromControl,
        {{TypeName}} toControl,
        {{ScalarType}} amount) =>
        Slerp(Slerp(from, to, amount), Slerp(fromControl, toControl, amount), {{TwoLiteral}} * ({{OneLiteral}} - amount) * amount);

    /// <summary>Creates a squad control point for the current quaternion.</summary>
    public static {{TypeName}} CreateSquadControlPoint({{TypeName}} previous, {{TypeName}} current, {{TypeName}} next)
    {
        var inverse = Invert(current);
        return Exp((Log(next * inverse) + Log(previous * inverse)) / -{{FourLiteral}}) * current;
    }

    /// <summary>Returns the quaternion exponential.</summary>
    public static {{TypeName}} Exp({{TypeName}} value)
    {
        var vectorLength = value.Vector.Length;
        var expScalar = ScalarMath.Exp(value.W);
        if (vectorLength <= {{ToleranceLiteral}})
            return new({{ZeroLiteral}}, {{ZeroLiteral}}, {{ZeroLiteral}}, expScalar);

        var scale = expScalar * ScalarMath.Sin(vectorLength) / vectorLength;
        return new(value.Vector * scale, expScalar * ScalarMath.Cos(vectorLength));
    }

    /// <summary>Returns the quaternion logarithm.</summary>
    public static {{TypeName}} Log({{TypeName}} value)
    {
        var vectorLength = value.Vector.Length;
        var quaternionLength = value.Length;
        if (vectorLength <= {{ToleranceLiteral}})
            return new({{ZeroLiteral}}, {{ZeroLiteral}}, {{ZeroLiteral}}, ScalarMath.Log(quaternionLength));

        var scale = ScalarMath.Atan2(vectorLength, value.W) / vectorLength;
        return new(value.Vector * scale, ScalarMath.Log(quaternionLength));
    }

    /// <summary>Raises a quaternion to a scalar power.</summary>
    public static {{TypeName}} Pow({{TypeName}} value, {{ScalarType}} exponent) =>
        ScalarMath.Abs(exponent) <= {{ToleranceLiteral}} ? Identity : Exp(Log(value) * exponent);

    /// <summary>Returns the quaternion square root.</summary>
    public static {{TypeName}} Sqrt({{TypeName}} value) =>
        Pow(value, {{HalfLiteral}});

    /// <summary>Returns a mask containing component-wise equality results.</summary>
    public static {{MaskType}} Equal({{TypeName}} left, {{TypeName}} right) =>
        {{Vector4Type}}.Equal(left.Components, right.Components);

    /// <summary>Returns a mask containing tolerance-based component-wise equality results.</summary>
    public static {{MaskType}} Equal({{TypeName}} left, {{TypeName}} right, {{ScalarType}} epsilon) =>
        new(
            ScalarMath.Abs(left.X - right.X) <= epsilon,
            ScalarMath.Abs(left.Y - right.Y) <= epsilon,
            ScalarMath.Abs(left.Z - right.Z) <= epsilon,
            ScalarMath.Abs(left.W - right.W) <= epsilon);

    /// <summary>Returns a mask containing component-wise inequality results.</summary>
    public static {{MaskType}} NotEqual({{TypeName}} left, {{TypeName}} right) =>
        {{Vector4Type}}.NotEqual(left.Components, right.Components);

    /// <summary>Returns a mask containing tolerance-based component-wise inequality results.</summary>
    public static {{MaskType}} NotEqual({{TypeName}} left, {{TypeName}} right, {{ScalarType}} epsilon) =>
        new(
            ScalarMath.Abs(left.X - right.X) > epsilon,
            ScalarMath.Abs(left.Y - right.Y) > epsilon,
            ScalarMath.Abs(left.Z - right.Z) > epsilon,
            ScalarMath.Abs(left.W - right.W) > epsilon);

    /// <summary>Returns a mask containing component-wise less-than results.</summary>
    public static {{MaskType}} LessThan({{TypeName}} left, {{TypeName}} right) =>
        {{Vector4Type}}.LessThan(left.Components, right.Components);

    /// <summary>Returns a mask containing component-wise less-than-or-equal results.</summary>
    public static {{MaskType}} LessThanOrEqual({{TypeName}} left, {{TypeName}} right) =>
        {{Vector4Type}}.LessThanOrEqual(left.Components, right.Components);

    /// <summary>Returns a mask containing component-wise greater-than results.</summary>
    public static {{MaskType}} GreaterThan({{TypeName}} left, {{TypeName}} right) =>
        {{Vector4Type}}.GreaterThan(left.Components, right.Components);

    /// <summary>Returns a mask containing component-wise greater-than-or-equal results.</summary>
    public static {{MaskType}} GreaterThanOrEqual({{TypeName}} left, {{TypeName}} right) =>
        {{Vector4Type}}.GreaterThanOrEqual(left.Components, right.Components);

    /// <summary>Returns a mask containing component-wise NaN results.</summary>
    public static {{MaskType}} IsNaN({{TypeName}} value) =>
        {{Vector4Type}}.IsNaN(value.Components);

    /// <summary>Returns a mask containing component-wise infinity results.</summary>
    public static {{MaskType}} IsInfinity({{TypeName}} value) =>
        {{Vector4Type}}.IsInfinity(value.Components);

    /// <summary>Returns a mask containing component-wise finite results.</summary>
    public static {{MaskType}} IsFinite({{TypeName}} value) =>
        {{Vector4Type}}.IsFinite(value.Components);

    /// <summary>Returns a mask containing component-wise less-than results.</summary>
    public static {{MaskType}} operator <({{TypeName}} left, {{TypeName}} right) =>
        LessThan(left, right);

    /// <summary>Returns a mask containing component-wise less-than-or-equal results.</summary>
    public static {{MaskType}} operator <=({{TypeName}} left, {{TypeName}} right) =>
        LessThanOrEqual(left, right);

    /// <summary>Returns a mask containing component-wise greater-than results.</summary>
    public static {{MaskType}} operator >({{TypeName}} left, {{TypeName}} right) =>
        GreaterThan(left, right);

    /// <summary>Returns a mask containing component-wise greater-than-or-equal results.</summary>
    public static {{MaskType}} operator >=({{TypeName}} left, {{TypeName}} right) =>
        GreaterThanOrEqual(left, right);

{{CrossScalarConversions}}{{SystemNumericsConversions}}    /// <summary>Converts a quaternion to an X/Y/Z/W tuple.</summary>
    public static implicit operator ({{ScalarType}} X, {{ScalarType}} Y, {{ScalarType}} Z, {{ScalarType}} W)({{TypeName}} value) =>
        (value.X, value.Y, value.Z, value.W);

    /// <summary>Converts an X/Y/Z/W tuple to a quaternion.</summary>
    public static implicit operator {{TypeName}}(({{ScalarType}} X, {{ScalarType}} Y, {{ScalarType}} Z, {{ScalarType}} W) value) =>
        new(value.X, value.Y, value.Z, value.W);

    /// <summary>Returns whether two quaternions have identical components.</summary>
    public static bool operator ==({{TypeName}} left, {{TypeName}} right) =>
        left.Equals(right);

    /// <summary>Returns whether two quaternions have any different component.</summary>
    public static bool operator !=({{TypeName}} left, {{TypeName}} right) =>
        !left.Equals(right);

    /// <summary>Returns a string representation of this quaternion.</summary>
    public override readonly string ToString() =>
        Components.ToString();

    /// <summary>Returns a formatted string representation of this quaternion.</summary>
    public readonly string ToString(string? format) =>
        Components.ToString(format);

    /// <summary>Returns a culture-aware string representation of this quaternion.</summary>
    public readonly string ToString(IFormatProvider? formatProvider) =>
        Components.ToString(formatProvider);

    /// <summary>Returns a formatted, culture-aware string representation of this quaternion.</summary>
    public readonly string ToString(string? format, IFormatProvider? formatProvider) =>
        Components.ToString(format, formatProvider);

    /// <summary>Attempts to format this quaternion into a caller-owned character span.</summary>
    public readonly bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider) =>
        Components.TryFormat(destination, out charsWritten, format, formatProvider);

    /// <summary>Attempts to format this quaternion into a caller-owned UTF-8 byte span.</summary>
    public readonly bool TryFormat(
        Span<byte> utf8Destination,
        out int bytesWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider) =>
        Components.TryFormat(utf8Destination, out bytesWritten, format, formatProvider);

    /// <summary>Compares this quaternion to another quaternion by component order.</summary>
    public readonly int CompareTo({{TypeName}} other) =>
        Components.CompareTo(other.Components);

    /// <summary>Parses a quaternion from a string.</summary>
    public static {{TypeName}} Parse(string s, IFormatProvider? formatProvider)
    {
        ArgumentNullException.ThrowIfNull(s);
        return Parse(s.AsSpan(), formatProvider);
    }

    /// <summary>Attempts to parse a quaternion from a string.</summary>
    public static bool TryParse(string? s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (s is null)
        {
            result = default;
            return false;
        }

        return TryParse(s.AsSpan(), formatProvider, out result);
    }

    /// <summary>Parses a quaternion from a character span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<char> s, IFormatProvider? formatProvider) =>
        TryParse(s, formatProvider, out var result)
            ? result
            : throw new FormatException("Input quaternion text was not in a correct format.");

    /// <summary>Attempts to parse a quaternion from a character span.</summary>
    public static bool TryParse(ReadOnlySpan<char> s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (!{{Vector4Type}}.TryParse(s, formatProvider, out var vector))
        {
            result = default;
            return false;
        }

        result = new(vector.X, vector.Y, vector.Z, vector.W);
        return true;
    }

    /// <summary>Parses a quaternion from a UTF-8 byte span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider) =>
        TryParse(utf8Text, formatProvider, out var result)
            ? result
            : throw new FormatException("Input quaternion text was not in a correct format.");

    /// <summary>Attempts to parse a quaternion from a UTF-8 byte span.</summary>
    public static bool TryParse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (!{{Vector4Type}}.TryParse(utf8Text, formatProvider, out var vector))
        {
            result = default;
            return false;
        }

        result = new(vector.X, vector.Y, vector.Z, vector.W);
        return true;
    }

    /// <summary>Returns whether this quaternion has the same components as another quaternion.</summary>
    public readonly bool Equals({{TypeName}} other) =>
        X.Equals(other.X) && Y.Equals(other.Y) && Z.Equals(other.Z) && W.Equals(other.W);

    /// <summary>Returns whether this quaternion has the same components as an object.</summary>
    public override readonly bool Equals(object? obj) =>
        obj is {{TypeName}} other && Equals(other);

    /// <summary>Returns a hash code for this quaternion.</summary>
    public override readonly int GetHashCode() =>
        HashCode.Combine(X, Y, Z, W);
}
