// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>Applies to all matrix types, including <see cref="Mat2" />, <see cref="Mat3x4" />, and <see cref="Mat4d" />.</summary>
/// <typeparam name="TSelf">The concrete matrix type, such as <see cref="Mat3" /> or <see cref="Mat4d" />.</typeparam>
/// <typeparam name="TScalar">The component type, such as <see cref="float" /> or <see cref="double" />.</typeparam>
/// <typeparam name="TColumn">The column vector type.</typeparam>
/// <typeparam name="TRow">The row vector type.</typeparam>
/// <typeparam name="TTranspose">The transposed matrix type.</typeparam>
public interface IMat<TSelf, TScalar, TColumn, TRow, TTranspose> :
    IEquatable<TSelf>,
    IComparable<TSelf>,
    IEqualityOperators<TSelf, TSelf, bool>,
    ISpanFormattable,
    IUtf8SpanFormattable,
    ISpanParsable<TSelf>,
    IUtf8SpanParsable<TSelf>
    where TSelf : struct, IMat<TSelf, TScalar, TColumn, TRow, TTranspose>
    where TColumn : struct, IVec<TColumn, TScalar>
    where TRow : struct, IVec<TRow, TScalar>
    where TTranspose : struct
{
    /// <summary>Gets the number of matrix columns.</summary>
    static abstract int ColumnCount { get; }

    /// <summary>Gets the number of matrix rows.</summary>
    static abstract int RowCount { get; }

    /// <summary>Gets the number of scalar components in the matrix.</summary>
    static abstract int ComponentCount { get; }

    /// <summary>Gets the byte size of the matrix.</summary>
    static abstract int SizeInBytes { get; }

    /// <summary>Gets the zero matrix.</summary>
    static abstract TSelf Zero { get; }

    /// <summary>Creates a matrix with diagonal components set to a scalar value.</summary>
    static abstract TSelf CreateDiagonal(TScalar diagonal);

    /// <summary>Creates a matrix from the outer product of a column vector and row vector.</summary>
    static abstract TSelf CreateOuterProduct(TColumn columnVector, TRow rowVector);

    /// <summary>Linearly interpolates between two matrices component by component.</summary>
    static abstract TSelf Lerp(TSelf from, TSelf to, TScalar amount);

    /// <summary>Creates a matrix from the first <see cref="ComponentCount" /> values in a column-major span.</summary>
    static abstract TSelf FromColumnMajor(ReadOnlySpan<TScalar> values);

    /// <summary>Creates a matrix from the first <see cref="ComponentCount" /> values in a row-major span.</summary>
    static abstract TSelf FromRowMajor(ReadOnlySpan<TScalar> values);

    /// <summary>Gets a mutable reference to a column by zero-based index.</summary>
    static abstract ref TColumn ColumnRef(ref TSelf value, int column);

    /// <summary>Gets a mutable reference to a scalar component by zero-based column and row indexes.</summary>
    static abstract ref TScalar ComponentRef(ref TSelf value, int column, int row);

    /// <summary>Returns the transpose of a matrix.</summary>
    static abstract TTranspose Transpose(TSelf value);

    /// <summary>Multiplies matrices component by component.</summary>
    static abstract TSelf ComponentMultiply(TSelf left, TSelf right);

    /// <summary>Transforms a column vector by a matrix.</summary>
    static abstract TColumn operator *(TSelf left, TRow right);

    /// <summary>Transforms a row vector by a matrix.</summary>
    static abstract TRow operator *(TColumn left, TSelf right);

    /// <summary>Gets the transposed matrix.</summary>
    TTranspose Transposed { get; }

    /// <summary>Gets or sets a column vector by zero-based index.</summary>
    TColumn this[int column] { get; set; }

    /// <summary>Gets or sets a scalar component by zero-based column and row indexes.</summary>
    TScalar this[int column, int row] { get; set; }

    /// <summary>Copies this matrix into an array in column-major order.</summary>
    void CopyTo(TScalar[] array);

    /// <summary>Copies this matrix into an array in column-major order starting at <paramref name="index" />.</summary>
    void CopyTo(TScalar[] array, int index);

    /// <summary>Copies this matrix into a destination span in column-major order.</summary>
    void CopyTo(Span<TScalar> destination);

    /// <summary>Copies this matrix into a destination span in column-major order.</summary>
    void CopyToColumnMajor(Span<TScalar> destination);

    /// <summary>Copies this matrix into a destination span in row-major order.</summary>
    void CopyToRowMajor(Span<TScalar> destination);

    /// <summary>Attempts to copy this matrix into a destination span in column-major order.</summary>
    bool TryCopyTo(Span<TScalar> destination);

    /// <summary>Attempts to copy this matrix into a destination span in column-major order.</summary>
    bool TryCopyToColumnMajor(Span<TScalar> destination);

    /// <summary>Attempts to copy this matrix into a destination span in row-major order.</summary>
    bool TryCopyToRowMajor(Span<TScalar> destination);
}
