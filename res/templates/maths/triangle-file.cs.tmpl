// <auto-generated/>
#nullable enable
namespace AlvorKit.Maths;

/// <summary>{{TypeSummary}}</summary>
/// <param name="a">The first triangle vertex.</param>
/// <param name="b">The second triangle vertex.</param>
/// <param name="c">The third triangle vertex.</param>
[StructLayout(LayoutKind.Sequential)]
[DebuggerDisplay("{ToString(),nq}")]
[ExcludeFromCodeCoverage]
public struct {{TypeName}}({{Vector3Type}} a, {{Vector3Type}} b, {{Vector3Type}} c)
    : {{ImplementedInterfaces}}
{
    /// <summary>The number of scalar components in a {{TypeName}}.</summary>
    public const int ComponentCount = 9;

    /// <summary>The byte size of a {{TypeName}}.</summary>
    public const int SizeInBytes = {{SizeBytes}};

    /// <summary>The first triangle vertex.</summary>
    public {{Vector3Type}} A = a;

    /// <summary>The second triangle vertex.</summary>
    public {{Vector3Type}} B = b;

    /// <summary>The third triangle vertex.</summary>
    public {{Vector3Type}} C = c;

    /// <summary>Gets or sets the first vertex through the generic triangle interface.</summary>
    {{Vector3Type}} ITriangle3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}},
        {{Ray3Type}}, {{Box3Type}}, {{Sphere3Type}}, {{Frustum3Type}}, {{IntervalType}}>.A
    {
        readonly get => A;
        set => A = value;
    }

    /// <summary>Gets or sets the second vertex through the generic triangle interface.</summary>
    {{Vector3Type}} ITriangle3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}},
        {{Ray3Type}}, {{Box3Type}}, {{Sphere3Type}}, {{Frustum3Type}}, {{IntervalType}}>.B
    {
        readonly get => B;
        set => B = value;
    }

    /// <summary>Gets or sets the third vertex through the generic triangle interface.</summary>
    {{Vector3Type}} ITriangle3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}},
        {{Ray3Type}}, {{Box3Type}}, {{Sphere3Type}}, {{Frustum3Type}}, {{IntervalType}}>.C
    {
        readonly get => C;
        set => C = value;
    }

    /// <summary>Gets the number of scalar components.</summary>
    static int ITriangle3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}}, {{Ray3Type}},
        {{Box3Type}}, {{Sphere3Type}}, {{Frustum3Type}}, {{IntervalType}}>.ComponentCount => ComponentCount;

    /// <summary>Gets the byte size.</summary>
    static int ITriangle3<{{TypeName}}, {{ScalarType}}, {{Vector3Type}}, {{Vector4Type}}, {{Plane3Type}}, {{Ray3Type}},
        {{Box3Type}}, {{Sphere3Type}}, {{Frustum3Type}}, {{IntervalType}}>.SizeInBytes => SizeInBytes;

    /// <summary>Initializes vertices from the first <see cref="ComponentCount" /> values in a span.</summary>
    public {{TypeName}}(ReadOnlySpan<{{ScalarType}}> values)
        : this(CreateVector(values, 0), CreateVector(values, 3), CreateVector(values, 6))
    {
    }

    /// <summary>Gets the edge from <see cref="A" /> to <see cref="B" />.</summary>
    public readonly {{Vector3Type}} EdgeAB => B - A;

    /// <summary>Gets the edge from <see cref="A" /> to <see cref="C" />.</summary>
    public readonly {{Vector3Type}} EdgeAC => C - A;

    /// <summary>Gets the edge from <see cref="B" /> to <see cref="C" />.</summary>
    public readonly {{Vector3Type}} EdgeBC => C - B;

    /// <summary>Gets the raw cross product of <see cref="EdgeAB" /> and <see cref="EdgeAC" />.</summary>
    public readonly {{Vector3Type}} UnnormalizedNormal => {{Vector3Type}}.Cross(EdgeAB, EdgeAC);

    /// <summary>Gets the unit normal, or throws when the triangle is degenerate.</summary>
    public readonly {{Vector3Type}} Normal =>
        TryGetNormal(out var normal) ? normal : throw new InvalidOperationException("The triangle is degenerate.");

    /// <summary>Gets the normalized plane containing this triangle, or throws when the triangle is degenerate.</summary>
    public readonly {{Plane3Type}} Plane =>
        TryGetPlane(out var plane) ? plane : throw new InvalidOperationException("The triangle is degenerate.");

    /// <summary>Gets the finite triangle area.</summary>
    public readonly {{ScalarType}} Area => UnnormalizedNormal.Length / {{TwoLiteral}};

    /// <summary>Gets whether the triangle has zero area.</summary>
    public readonly bool IsDegenerate => UnnormalizedNormal.LengthSquared == {{ZeroLiteral}};

    /// <summary>Creates a triangle from three vertices.</summary>
    public static {{TypeName}} Create({{Vector3Type}} a, {{Vector3Type}} b, {{Vector3Type}} c) =>
        new(a, b, c);

    /// <summary>Creates a triangle from the first <see cref="ComponentCount" /> scalar values in a span.</summary>
    public static {{TypeName}} Create(ReadOnlySpan<{{ScalarType}}> values) =>
        new(values);

    /// <summary>Gets or sets a scalar component by zero-based index, with vertex components in A, B, C order.</summary>
    public {{ScalarType}} this[int index]
    {
        readonly get
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            if (index < 3)
                return A[index];
            if (index < 6)
                return B[index - 3];
            return C[index - 6];
        }
        set
        {
            if ((uint)index >= ComponentCount)
                MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

            if (index < 3)
                A[index] = value;
            else if (index < 6)
                B[index - 3] = value;
            else
                C[index - 6] = value;
        }
    }

    /// <summary>Gets a mutable reference to a scalar component by zero-based index.</summary>
    public static ref {{ScalarType}} ComponentRef(ref {{TypeName}} value, int index)
    {
        if ((uint)index >= ComponentCount)
            MathsThrowHelper.ThrowComponentIndexOutOfRange(index, ComponentCount - 1);

        if (index < 3)
            return ref {{Vector3Type}}.ComponentRef(ref value.A, index);
        if (index < 6)
            return ref {{Vector3Type}}.ComponentRef(ref value.B, index - 3);
        return ref {{Vector3Type}}.ComponentRef(ref value.C, index - 6);
    }

    /// <summary>Deconstructs the triangle into vertex components.</summary>
    public readonly void Deconstruct(out {{Vector3Type}} a, out {{Vector3Type}} b, out {{Vector3Type}} c)
    {
        a = A;
        b = B;
        c = C;
    }

    /// <summary>Copies vertex components into a caller-owned span in A, B, C order.</summary>
    public readonly void CopyTo(Span<{{ScalarType}}> destination)
    {
        if (!TryCopyTo(destination))
            MathsThrowHelper.ThrowDestinationSpanTooShort(ComponentCount);
    }

    /// <summary>Attempts to copy vertex components into a caller-owned span in A, B, C order.</summary>
    public readonly bool TryCopyTo(Span<{{ScalarType}}> destination)
    {
        if (destination.Length < ComponentCount)
            return false;

        A.CopyTo(destination);
        B.CopyTo(destination[3..]);
        C.CopyTo(destination[6..]);
        return true;
    }

    /// <summary>Returns the unit normal when the triangle has nonzero area.</summary>
    public readonly bool TryGetNormal(out {{Vector3Type}} normal) =>
        UnnormalizedNormal.TryNormalize(out normal);

    /// <summary>Returns the normalized plane containing this triangle when the triangle has nonzero area.</summary>
    public readonly bool TryGetPlane(out {{Plane3Type}} plane)
    {
        if (!TryGetNormal(out var normal))
        {
            plane = default;
            return false;
        }

        plane = {{Plane3Type}}.CreateFromPointNormal(A, normal);
        return true;
    }

    /// <summary>Returns barycentric coordinates for <paramref name="point" /> relative to this triangle.</summary>
    public readonly {{Vector3Type}} Barycentric({{Vector3Type}} point)
    {
        var v0 = EdgeAB;
        var v1 = EdgeAC;
        var v2 = point - A;
        var d00 = {{Vector3Type}}.Dot(v0, v0);
        var d01 = {{Vector3Type}}.Dot(v0, v1);
        var d11 = {{Vector3Type}}.Dot(v1, v1);
        var d20 = {{Vector3Type}}.Dot(v2, v0);
        var d21 = {{Vector3Type}}.Dot(v2, v1);
        var denominator = (d00 * d11) - (d01 * d01);
        if (denominator == {{ZeroLiteral}})
            throw new InvalidOperationException("The triangle is degenerate.");

        var v = ((d11 * d20) - (d01 * d21)) / denominator;
        var w = ((d00 * d21) - (d01 * d20)) / denominator;
        var u = {{OneLiteral}} - v - w;
        return new {{Vector3Type}}(u, v, w);
    }

    /// <summary>Returns whether this triangle contains <paramref name="point" />.</summary>
    public readonly bool Contains({{Vector3Type}} point)
    {
        if (IsDegenerate || Plane.Evaluate(point) != {{ZeroLiteral}})
            return false;

        var barycentric = Barycentric(point);
        return barycentric.X >= {{ZeroLiteral}} && barycentric.Y >= {{ZeroLiteral}} && barycentric.Z >= {{ZeroLiteral}};
    }

    /// <summary>Returns whether this triangle intersects <paramref name="box" />.</summary>
    public readonly bool Intersects({{Box3Type}} box) =>
        !IsDegenerate && !box.IsEmpty && IntersectsTriangleBox(box);

    /// <summary>Returns whether this triangle intersects <paramref name="sphere" />.</summary>
    public readonly bool Intersects({{Sphere3Type}} sphere) =>
        !sphere.IsEmpty && DistanceSquaredTo(sphere.Center) <= sphere.RadiusSquared;

    /// <summary>Returns whether <paramref name="ray" /> intersects this triangle at a nonnegative distance.</summary>
    public readonly bool Intersects({{Ray3Type}} ray) =>
        TryIntersect(ray, out _);

    /// <summary>Attempts to find the nearest nonnegative intersection distance from <paramref name="ray" /> to this triangle.</summary>
    public readonly bool TryIntersect({{Ray3Type}} ray, out {{ScalarType}} distance)
    {
        distance = default;
        var edge1 = EdgeAB;
        var edge2 = EdgeAC;
        var p = {{Vector3Type}}.Cross(ray.Direction, edge2);
        var determinant = {{Vector3Type}}.Dot(edge1, p);
        if (determinant == {{ZeroLiteral}})
            return false;

        var inverseDeterminant = {{OneLiteral}} / determinant;
        var t = ray.Origin - A;
        var u = {{Vector3Type}}.Dot(t, p) * inverseDeterminant;
        if (u < {{ZeroLiteral}} || u > {{OneLiteral}})
            return false;

        var q = {{Vector3Type}}.Cross(t, edge1);
        var v = {{Vector3Type}}.Dot(ray.Direction, q) * inverseDeterminant;
        if (v < {{ZeroLiteral}} || u + v > {{OneLiteral}})
            return false;

        distance = {{Vector3Type}}.Dot(edge2, q) * inverseDeterminant;
        if (distance < {{ZeroLiteral}})
        {
            distance = default;
            return false;
        }

        return true;
    }

    /// <summary>Returns whether this triangle intersects <paramref name="box" /> using separating axis tests.</summary>
    private readonly bool IntersectsTriangleBox({{Box3Type}} box)
    {
        var center = box.Center;
        var halfSize = box.HalfSize;
        var a = A - center;
        var b = B - center;
        var c = C - center;
        var ab = b - a;
        var bc = c - b;
        var ca = a - c;

        if (SeparatesOnBoxAxis(a.X, b.X, c.X, halfSize.X) ||
            SeparatesOnBoxAxis(a.Y, b.Y, c.Y, halfSize.Y) ||
            SeparatesOnBoxAxis(a.Z, b.Z, c.Z, halfSize.Z))
        {
            return false;
        }

        if (SeparatesOnAxis({{Vector3Type}}.Cross(ab, EdgeAC), a, b, c, halfSize))
            return false;

        return !SeparatesOnAxis(new {{Vector3Type}}({{ZeroLiteral}}, ab.Z, -ab.Y), a, b, c, halfSize) &&
            !SeparatesOnAxis(new {{Vector3Type}}(-ab.Z, {{ZeroLiteral}}, ab.X), a, b, c, halfSize) &&
            !SeparatesOnAxis(new {{Vector3Type}}(ab.Y, -ab.X, {{ZeroLiteral}}), a, b, c, halfSize) &&
            !SeparatesOnAxis(new {{Vector3Type}}({{ZeroLiteral}}, bc.Z, -bc.Y), a, b, c, halfSize) &&
            !SeparatesOnAxis(new {{Vector3Type}}(-bc.Z, {{ZeroLiteral}}, bc.X), a, b, c, halfSize) &&
            !SeparatesOnAxis(new {{Vector3Type}}(bc.Y, -bc.X, {{ZeroLiteral}}), a, b, c, halfSize) &&
            !SeparatesOnAxis(new {{Vector3Type}}({{ZeroLiteral}}, ca.Z, -ca.Y), a, b, c, halfSize) &&
            !SeparatesOnAxis(new {{Vector3Type}}(-ca.Z, {{ZeroLiteral}}, ca.X), a, b, c, halfSize) &&
            !SeparatesOnAxis(new {{Vector3Type}}(ca.Y, -ca.X, {{ZeroLiteral}}), a, b, c, halfSize);
    }

    /// <summary>Returns whether projected triangle coordinates are disjoint from a centered box interval.</summary>
    private static bool SeparatesOnBoxAxis({{ScalarType}} a, {{ScalarType}} b, {{ScalarType}} c, {{ScalarType}} halfSize) =>
        ScalarMath.Min(a, ScalarMath.Min(b, c)) > halfSize || ScalarMath.Max(a, ScalarMath.Max(b, c)) < -halfSize;

    /// <summary>Returns whether <paramref name="axis" /> separates a triangle from a centered box.</summary>
    private static bool SeparatesOnAxis(
        {{Vector3Type}} axis,
        {{Vector3Type}} a,
        {{Vector3Type}} b,
        {{Vector3Type}} c,
        {{Vector3Type}} halfSize)
    {
        var radius = (halfSize.X * ScalarMath.Abs(axis.X)) +
            (halfSize.Y * ScalarMath.Abs(axis.Y)) +
            (halfSize.Z * ScalarMath.Abs(axis.Z));
        var projectionA = {{Vector3Type}}.Dot(a, axis);
        var projectionB = {{Vector3Type}}.Dot(b, axis);
        var projectionC = {{Vector3Type}}.Dot(c, axis);
        return ScalarMath.Min(projectionA, ScalarMath.Min(projectionB, projectionC)) > radius ||
            ScalarMath.Max(projectionA, ScalarMath.Max(projectionB, projectionC)) < -radius;
    }

    /// <summary>Returns the closest point on this finite triangle to <paramref name="point" />.</summary>
    public readonly {{Vector3Type}} ClosestPoint({{Vector3Type}} point)
    {
        var ab = EdgeAB;
        var ac = EdgeAC;
        var ap = point - A;
        var d1 = {{Vector3Type}}.Dot(ab, ap);
        var d2 = {{Vector3Type}}.Dot(ac, ap);
        if (d1 <= {{ZeroLiteral}} && d2 <= {{ZeroLiteral}})
            return A;

        var bp = point - B;
        var d3 = {{Vector3Type}}.Dot(ab, bp);
        var d4 = {{Vector3Type}}.Dot(ac, bp);
        if (d3 >= {{ZeroLiteral}} && d4 <= d3)
            return B;

        var vc = (d1 * d4) - (d3 * d2);
        if (vc <= {{ZeroLiteral}} && d1 >= {{ZeroLiteral}} && d3 <= {{ZeroLiteral}})
        {
            var v = d1 / (d1 - d3);
            return A + (ab * v);
        }

        var cp = point - C;
        var d5 = {{Vector3Type}}.Dot(ab, cp);
        var d6 = {{Vector3Type}}.Dot(ac, cp);
        if (d6 >= {{ZeroLiteral}} && d5 <= d6)
            return C;

        var vb = (d5 * d2) - (d1 * d6);
        if (vb <= {{ZeroLiteral}} && d2 >= {{ZeroLiteral}} && d6 <= {{ZeroLiteral}})
        {
            var w = d2 / (d2 - d6);
            return A + (ac * w);
        }

        var va = (d3 * d6) - (d5 * d4);
        if (va <= {{ZeroLiteral}} && d4 - d3 >= {{ZeroLiteral}} && d5 - d6 >= {{ZeroLiteral}})
        {
            var w = (d4 - d3) / ((d4 - d3) + (d5 - d6));
            return B + (EdgeBC * w);
        }

        var denominator = {{OneLiteral}} / (va + vb + vc);
        var y = vb * denominator;
        var z = vc * denominator;
        return A + (ab * y) + (ac * z);
    }

    /// <summary>Returns the distance from this finite triangle to <paramref name="point" />.</summary>
    public readonly {{ScalarType}} DistanceTo({{Vector3Type}} point) =>
        {{Vector3Type}}.Distance(point, ClosestPoint(point));

    /// <summary>Returns the squared distance from this finite triangle to <paramref name="point" />.</summary>
    public readonly {{ScalarType}} DistanceSquaredTo({{Vector3Type}} point) =>
        {{Vector3Type}}.DistanceSquared(point, ClosestPoint(point));

{{CrossScalarConversions}}
    /// <summary>Creates a triangle from an A/B/C vertex tuple.</summary>
    public static implicit operator {{TypeName}}(({{Vector3Type}} A, {{Vector3Type}} B, {{Vector3Type}} C) value) =>
        new(value.A, value.B, value.C);

    /// <summary>Returns this triangle as an A/B/C vertex tuple.</summary>
    public static implicit operator ({{Vector3Type}} A, {{Vector3Type}} B, {{Vector3Type}} C)({{TypeName}} value) =>
        (value.A, value.B, value.C);

    /// <summary>Returns whether two triangles have the same vertices.</summary>
    public static bool operator ==({{TypeName}} left, {{TypeName}} right) =>
        left.Equals(right);

    /// <summary>Returns whether two triangles have different vertices.</summary>
    public static bool operator !=({{TypeName}} left, {{TypeName}} right) =>
        !left.Equals(right);

    /// <summary>Returns a string representation of this triangle.</summary>
    public override readonly string ToString() =>
        ToString(null, null);

    /// <summary>Returns a formatted string representation of this triangle.</summary>
    public readonly string ToString(string? format) =>
        ToString(format, null);

    /// <summary>Returns a culture-aware string representation of this triangle.</summary>
    public readonly string ToString(IFormatProvider? formatProvider) =>
        ToString(null, formatProvider);

    /// <summary>Returns a formatted, culture-aware string representation of this triangle.</summary>
    public readonly string ToString(string? format, IFormatProvider? formatProvider)
    {
        Span<char> destination = stackalloc char[768];
        return TryFormat(destination, out var charsWritten, format.AsSpan(), formatProvider)
            ? new string(destination[..charsWritten])
            : MathsFormatHelper.ToStringSlow(this, format, formatProvider, 1536);
    }

    /// <summary>Attempts to format this triangle into a caller-owned character span.</summary>
    public readonly bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        charsWritten = 0;
        var remainder = destination;

        if (!MathsFormatHelper.TryAppend("(".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(A, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(", ".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(B, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(", ".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppendFormatted(C, ref remainder, ref charsWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out charsWritten);
        if (!MathsFormatHelper.TryAppend(")".AsSpan(), ref remainder, ref charsWritten))
            return MathsFormatHelper.Fail(out charsWritten);

        return true;
    }

    /// <summary>Attempts to format this triangle into a caller-owned UTF-8 byte span.</summary>
    public readonly bool TryFormat(
        Span<byte> utf8Destination,
        out int bytesWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? formatProvider)
    {
        bytesWritten = 0;
        var remainder = utf8Destination;

        if (!MathsFormatHelper.TryAppend("("u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(A, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(", "u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(B, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(", "u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppendFormatted(C, ref remainder, ref bytesWritten, format, formatProvider))
            return MathsFormatHelper.Fail(out bytesWritten);
        if (!MathsFormatHelper.TryAppend(")"u8, ref remainder, ref bytesWritten))
            return MathsFormatHelper.Fail(out bytesWritten);

        return true;
    }

    /// <summary>Compares this triangle to another triangle by vertex order.</summary>
    public readonly int CompareTo({{TypeName}} other)
    {
        var aComparison = A.CompareTo(other.A);
        if (aComparison != 0)
            return aComparison;

        var bComparison = B.CompareTo(other.B);
        if (bComparison != 0)
            return bComparison;

        return C.CompareTo(other.C);
    }

    /// <summary>Parses a triangle from a string.</summary>
    public static {{TypeName}} Parse(string s, IFormatProvider? formatProvider)
    {
        ArgumentNullException.ThrowIfNull(s);
        return Parse(s.AsSpan(), formatProvider);
    }

    /// <summary>Attempts to parse a triangle from a string.</summary>
    public static bool TryParse(string? s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        if (s is null)
        {
            result = default;
            return false;
        }

        return TryParse(s.AsSpan(), formatProvider, out result);
    }

    /// <summary>Parses a triangle from a character span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<char> s, IFormatProvider? formatProvider) =>
        TryParse(s, formatProvider, out var result)
            ? result
            : throw new FormatException("Input triangle text was not in a correct format.");

    /// <summary>Attempts to parse a triangle from a character span.</summary>
    public static bool TryParse(ReadOnlySpan<char> s, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(s, out var body))
            return false;

        if (!MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} a) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} b) ||
            !{{Vector3Type}}.TryParse(body.Trim(), formatProvider, out var c))
        {
            return false;
        }

        result = new(a, b, c);
        return true;
    }

    /// <summary>Parses a triangle from a UTF-8 byte span.</summary>
    public static {{TypeName}} Parse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider) =>
        TryParse(utf8Text, formatProvider, out var result)
            ? result
            : throw new FormatException("Input triangle text was not in a correct format.");

    /// <summary>Attempts to parse a triangle from a UTF-8 byte span.</summary>
    public static bool TryParse(ReadOnlySpan<byte> utf8Text, IFormatProvider? formatProvider, out {{TypeName}} result)
    {
        result = default;
        if (!MathsParseHelper.TryReadParenthesizedBody(utf8Text, out var body))
            return false;

        if (!MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} a) ||
            !MathsComponentParseHelper.TryReadNextTopLevelComponent(ref body, formatProvider, out {{Vector3Type}} b) ||
            !{{Vector3Type}}.TryParse(MathsUtf8TextHelper.TrimAsciiWhitespace(body), formatProvider, out var c))
        {
            return false;
        }

        result = new(a, b, c);
        return true;
    }

    /// <summary>Returns whether this triangle has the same vertices as another triangle.</summary>
    public readonly bool Equals({{TypeName}} other) =>
        A.Equals(other.A) && B.Equals(other.B) && C.Equals(other.C);

    /// <summary>Returns whether this triangle has the same vertices as an object.</summary>
    public override readonly bool Equals(object? obj) =>
        obj is {{TypeName}} other && Equals(other);

    /// <summary>Returns a hash code for this triangle.</summary>
    public override readonly int GetHashCode() =>
        HashCode.Combine(A, B, C);

    /// <summary>Creates a vector from span components.</summary>
    private static {{Vector3Type}} CreateVector(ReadOnlySpan<{{ScalarType}}> values, int offset)
    {
        if (values.Length < ComponentCount)
            MathsThrowHelper.ThrowSourceSpanTooShort(ComponentCount);

        return new(values[offset], values[offset + 1], values[offset + 2]);
    }
}
