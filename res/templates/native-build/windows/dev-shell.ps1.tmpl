$env:PATH = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer;$env:PATH"
$VsPath = & vswhere.exe -latest -products * -requires {{RequiredComponents}} -property installationPath
if (-not $VsPath) { throw {{MissingMessage}} }
& "$VsPath\Common7\Tools\Launch-VsDevShell.ps1" -Arch {{VisualStudioArchitecture}} -SkipAutomaticLocation | Out-Null
{{ClangClCheck}}
